# v4.11.0 — Editor UX + frugal AI (Track B2) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. TDD per item (failing test → run/confirm fail → impl → run/confirm pass → commit). Pure-PHP CLI harness (`php tests/<file>.php`; new tests get the `PHP_SAPI` 404 guard + `ABSPATH` define + end with a `Result: N passed, M failed.` line + `exit($fail>0?1:0)`). JS-only items are PHP-wiring-tested + a manual-UAT checklist. All MINOR → **v4.11.0**.

**Goal:** Ship the 5-item "Editor UX + frugal AI" bundle: content-aware Insights advisor, a pre-publish mistake gate, an expanded ⌘K palette, an AI release-notes drafter, and a "Create draft" button on Insights cards.

**Architecture:** Surgical additions to existing SN-owned subsystems (Insights, command palette, admin-post dispatcher, abilities) + two new editor-JS surfaces (no JSX, classic-script IIFE like `command-palette.js`). Items 1 + 5 add zero AI calls; 2 + 3 are client-side only; 4 makes one on-demand, input-capped call.

**Tech Stack:** PHP 8.0+, WordPress 7.0 (`PluginPrePublishPanel`, `@wordpress/commands`), vendored `wp-ai-client` (`claude-sonnet-4-6` pin).

**Baseline:** 50 suites / 1612 assertions / 0 failures; phpcs clean. Worktree `claude/v4.11.0` off `26905b6` (v4.10.1). Read the REAL source before each edit. Spec: `docs/superpowers/specs/2026-06-07-b2-editor-ux-frugal-ai-design.md`.

---

## Task 1 — Insights body-grounding (zero new AI calls)
**Files:** Modify `inc/insights.php` (consts ~32-36; `snt_insights_collect_signals()` after the sort+slice at ~143-151; one sentence in `snt_insights_system_instruction()` ~242-267). Test: extend `tests/insights.php`.
**Change:**
- Add consts: `SN_INSIGHTS_EXCERPT_CAP = 25`, `SN_INSIGHTS_EXCERPT_WORDS = 120`, `SN_INSIGHTS_EXCERPT_TOTAL_CHARS = 60000`.
- After the existing `views_7d`-desc / `days_since_publish`-asc sort + `SN_INSIGHTS_POST_CAP` slice, walk the first `SN_INSIGHTS_EXCERPT_CAP` `$out['posts']` and attach `$post['excerpt']`: prefer a trimmed author `post_excerpt` (whitespace-only ⇒ empty), else `snt_ai_extract_post_text($id, SN_INSIGHTS_EXCERPT_WORDS)` (ai-bootstrap.php:267-280). Track a running char total; stop attaching once `SN_INSIGHTS_EXCERPT_TOTAL_CHARS` is exceeded.
- Add ONE sentence to the system instruction noting the top posts now carry a content excerpt. Keep minimal (do not disturb the 5-rec output shape / `snt_insights_parse_response`).
- Optional: `excerpts_count` in `signal_summary` (~455-459).
**Test (extend `tests/insights.php`):** add `strip_shortcodes` / `wp_strip_all_tags` / `wp_trim_words` stubs + `post_content` (+ some `post_excerpt`) on fixtures. Assert: top-25 carry `excerpt`; post #26 does not; whitespace-only `post_excerpt` falls back to body-derived; an excerpt respects the word cap; the total-chars ceiling truncates the set (seed enough long bodies to trip it).
**Commit:** `feat(insights): body-ground the weekly advisor with bounded top-25 excerpts`
**SETTINGS:** none.

## Task 2 — Pre-publish mistake gate (client-side, no AI/network)
**Files:** Create `inc/pre-publish-gate.php` + `assets/pre-publish-gate.js`; modify `signal-and-noise-tools.php` (require ~line 180). Test: `tests/pre-publish-gate.php` (NEW).
**Change:**
- `inc/pre-publish-gate.php` (~45 lines, mirror `inc/command-palette.php`): `add_action('admin_enqueue_scripts', …)` gated on `$hook_suffix` `post.php`||`post-new.php` AND `current_user_can('edit_posts')`. Register `snt-pre-publish-gate` (`assets/pre-publish-gate.js`, deps `wp-plugins, wp-editor, wp-data, wp-element, wp-i18n`, `SNT_VERSION`, in_footer=true). Do NOT gate on `snt_ai_is_available()`.
- `assets/pre-publish-gate.js` (~80 lines, no JSX): guard `if (!window.wp || !wp.plugins || !wp.editor) return;`. `wp.plugins.registerPlugin('snt-pre-publish-gate', { render })`; `render` returns a `PluginPrePublishPanel` (from `wp.editor`) whose body lists advisory warnings computed from `wp.data.select('core/editor')` (post type, `meta._sn_noindex`, `meta._sn_meta_description`, tag count via `getEditedPostAttribute('tags')`). Use `wp.element.createElement`, `wp.i18n.__`. Checks: noindex-on (post & page); empty meta-desc (post & page); zero tags (post only). Advisory only (no `lockPostSaving`).
- `require_once` in `signal-and-noise-tools.php` near the editor includes (~180).
**Test (`tests/pre-publish-gate.php`):** stub `add_action` / `wp_register_script` / `wp_enqueue_script` / `current_user_can` capture. Assert: the enqueue callback registers `snt-pre-publish-gate` with the right deps; only on `post.php`/`post-new.php`; only when `edit_posts`; does NOT depend on AI availability. (JS logic → manual-UAT checklist in the spec verify gate.)
**Commit:** `feat(editor): pre-publish advisory gate (noindex / meta-desc / tags)`

## Task 3 — Expand the ⌘K command palette (client-side)
**Files:** Modify `inc/command-palette.php` (localize payload) + `assets/command-palette.js`. Test: `tests/command-palette-localize.php` (NEW).
**Change:**
- `inc/command-palette.php`: extend `wp_localize_script` with `newNoteUrl => admin_url('post-new.php')`; `tabs => [{label,url}]` built from `sn_admin_top_tabs()` (guard `function_exists`) → each `admin_url('admin.php?page='.$slug)`; `notesCategoryId => (int)` via `get_term_by('slug', SN_NOTES_CATEGORY_SLUG, 'category')` (guard the const + function; `0` when unseeded).
- `assets/command-palette.js`: register `New Note` (→ `newNoteUrl`), 6 `SN: Go to <Tab>` commands (from `tabs`), and — only when `notesCategoryId > 0` — up to 5 recent Notes via ONE `wp.apiFetch({ path: '/wp/v2/posts?categories='+id+'&per_page=5&status=any' })`, each command navigating to `post.php?post=<id>&action=edit`, the label **entity-decoded** from `title.rendered` and passed as plain text. Keep the existing 5 ability commands + the `manage_options`/WP-7.0 no-op guards.
**Test (`tests/command-palette-localize.php`):** stub `sn_admin_top_tabs` + `get_term_by` + `admin_url` + `wp_localize_script` capture. Assert the payload has `newNoteUrl`, a `tabs` array mirroring the SSOT, and `notesCategoryId` = the term id (and `0` when `get_term_by` returns false). (JS → manual-UAT checklist.)
**Commit:** `feat(palette): New-Note + tab-jumps + recent Notes in the ⌘K palette`

## Task 4 — AI release-notes drafter (Tools sub-tab + ability)
**Files:** Create `inc/release-notes-draft.php` + `inc/admin-forms/release-notes.php`; modify `inc/admin-tabs-data.php` (~104), `inc/admin-page.php` (~238), `inc/admin-post-actions.php` (callback), `inc/admin-post-handler.php:29` (map), `inc/abilities-system.php` (ability) + `inc/abilities-registration.php` (count docblock 42→43), `signal-and-noise-tools.php` (requires). Test: `tests/release-notes-draft.php` (NEW) + extend `tests/admin-post-actions.php`.
**Change:**
- `inc/release-notes-draft.php`: `snt_release_notes_draft_impl(string $changelog_delta): string|WP_Error`. `$gate = snt_ai_require_text_generation(); if ($gate) return $gate;`. Trim; empty ⇒ `WP_Error('snt_rn_empty', …, ['status'=>422])`. Hard-cap input to ~4000 chars BEFORE the call (word-boundary). Const `SNT_RELEASE_NOTES_SYSTEM` = Mimestream instruction (emit only `### New` / `### Improvements` / `### Fixed`; concise; never invent changes). Call `snt_ai_generate_with_constraints($delta, SNT_RELEASE_NOTES_SYSTEM, 700)`; return the markdown string (or its `WP_Error`).
- `inc/admin-forms/release-notes.php`: `sn_admin_render_release_notes_section()` — a `<textarea name="changelog_delta">` + submit (hidden `sn_action=release_notes_draft` + `wp_nonce_field('sn_theme_options_nonce')`); render the last result (from a transient/flash) in a copyable `<textarea readonly>`.
- `inc/admin-tabs-data.php`: add a `release-notes` sub-tab under Tools (~104). ⚠ Tools 4→5 sub-tabs — keep in-page tab count == submenu count ([[feedback_desktop_mode_horizontal_submenu_warning]]).
- `inc/admin-page.php`: dispatch the `release-notes` sub-tab render (~238).
- `inc/admin-post-actions.php`: `sn_handle_release_notes_draft()` — cap/validate, call the impl, stash the result (or error) in a short transient keyed for redisplay, return a flash key. `inc/admin-post-handler.php:29`: map `'release_notes_draft' => 'sn_handle_release_notes_draft'`.
- `inc/abilities-system.php`: register `signal-noise/draft-release-notes` (category `diagnostics`, perm `snt_ability_perm_manage_options`, `show_in_rest`, input `{changelog_delta: string}`, annotations `readonly`=true **`idempotent`=false**), execute → `snt_release_notes_draft_impl`. Update `inc/abilities-registration.php` Total docblock 42→43 + the abilities-system per-file count.
- `signal-and-noise-tools.php`: `require_once` the impl + wire the admin-form include.
**Test (`tests/release-notes-draft.php`):** stub `snt_ai_require_text_generation` (null) + `snt_ai_generate_with_constraints` (capture args, return a canned markdown). Assert: empty/whitespace ⇒ `snt_rn_empty`; an over-length delta is truncated before the call (assert the captured prompt length); the system instruction is passed; a normal delta returns the canned markdown. `tests/admin-post-actions.php`: handler-map count +1 and `release_notes_draft` is callable.
**Commit:** `feat(release-notes): AI release-notes drafter (Tools sub-tab + read-only ability)`

## Task 5 — "Create draft" button on Insights write_about cards (zero new AI calls)
**Files:** Modify `inc/insights.php` (`snt_insights_find_rec` + `snt_insights_create_draft_from_rec`), `inc/insights-admin.php` (button on write_about cards), `inc/admin-post-actions.php` (callback), `inc/admin-post-handler.php:29` (map), `inc/admin-flash-messages.php` (success flash). Test: extend `tests/insights.php` + `tests/admin-post-actions.php`.
**Change:**
- `inc/insights.php`: `snt_insights_find_rec($rec_id)` — read `snt_insights_last_scan()`, return the matching `recommendations[]` entry or null (cache miss/expired ⇒ null). `snt_insights_create_draft_from_rec($rec): int|WP_Error` — build `$postarr` (`post_status=draft`, `post_type=post`, title from the rec, `post_content` = the rationale wrapped as a valid `<!-- wp:paragraph --><p>…</p><!-- /wp:paragraph -->` block), assign the Notes category (resolve its term id; skip if unseeded), `wp_insert_post($postarr, true)`.
- `inc/insights-admin.php`: on `write_about` cards only, render a "Create draft" form (hidden `sn_action=insights_create_draft`, `rec_id`, `wp_nonce_field`).
- `inc/admin-post-actions.php`: `sn_handle_insights_create_draft()` — `find_rec` (miss ⇒ error flash), `create_draft_from_rec`, mark the rec done on success, set a success flash carrying `get_edit_post_link($id)`. **Option A redirect** — let the shared dispatcher PRG-redirect back to the Insights tab (do NOT modify it). `inc/admin-post-handler.php:29`: map `'insights_create_draft' => 'sn_handle_insights_create_draft'`. `inc/admin-flash-messages.php`: a success notice rendering the "draft created — edit it" link.
**Test (`tests/insights.php` + `tests/admin-post-actions.php`):** `find_rec` hit/miss/expired; `create_draft_from_rec` returns a `$postarr` with `post_status=draft`, Notes category, and a VALID `wp:paragraph` block body; handler-map +1.
**Commit:** `feat(insights): "Create draft" button seeds a Note from a cached recommendation`

## Task 6 — Version + CHANGELOG
Bump `Version: 4.10.1` → `4.11.0`. CHANGELOG `[4.11.0]` (Mimestream): **New** — pre-publish mistake gate; expanded ⌘K palette (New-Note, tab-jumps, recent Notes); AI release-notes drafter; Insights "Create draft". **Improvements** — Insights advisor is now content-aware (body-grounded, no extra cost).
**Commit:** `chore(release): v4.11.0 — Editor UX + frugal AI`

## Task 7 — Verify gate
Full sweep (excl `contracts-smoke.php`) → 0 failures (50 + new/extended suites). `composer run lint` clean + a falsification probe on a new file (phpcs `parallel=8` shows "8/8" batches not files — [[reference_phpcs_parallel_batches_not_files]]). Manual UAT: trigger each pre-publish warning; ⌘K → New-Note + a tab-jump + a recent Note; draft release notes from a pasted delta; Create-draft on a write_about card → lands a Notes draft + the flash links to its editor.

## Self-review
5 items → T1–T5. Items 1 + 5 add zero AI calls; 2 + 3 client-side only; 4 = one on-demand input-capped call (annotations `idempotent=false`). No `ai/ai` / desktop-mode overlap (grounding `wff02fw1h`). Ability count 42→43 (T4). New `perf`-style settings subtree: NONE (no settings added). No new dashboard widget / top-level admin-bar node. Editor-JS items have PHP-wiring tests + a manual-UAT checklist (no JS harness — keep the JS logic trivial + pure).
