# The two hosts: S&N Dashboard and S&N Analytics on the App Framework

**Date:** 2026-09-06 · **Owner decision (2026-09-05):** "adapt SN Dashboard and S&N Analytics to the App Framework — PORT = FAITHFUL. ALWAYS. Host first (framework tabs + server views calling the existing leaf renderers; no iframe), rebuild the daily surfaces second." · **Issues:** #1074 (Dashboard), #1075 (Analytics) · **Cuts:** two MINORs, one per host.

## What a host is

A native OpenStation window (`App::define()`, server views) that paints the SAME HTML the classic admin page paints, produced by the SAME render callables, for every leaf, with every form saving through the SAME handler table and every notice saying the same thing. Nothing is redesigned, dropped or "simplified"; the window is the admin page wearing the shell's chrome. The daily-surface rebuilds (client views in the Explorer idiom) come after, and only for surfaces the owner names.

## Measured facts the design rests on (2026-09-06)

1. **The desktop document IS wp-admin.** `/openstation/` resolves to `wp-admin/admin.php?page=openstation`; `body.wp-admin.wp-core-ui`; core's `common`, `forms`, `dashboard`, `list-tables`, `buttons`, `edit`, `media` stylesheets are loaded (measured: a probe `.form-table th` computes 257px/10px/left, `.postbox` has its 1px border, `.button-primary` its background and 2px radius, `.notice` its 4px left border). The leaves' admin HTML styles natively inside a window. **No iframe is needed and none is used.**
2. **The Dashboard's IA is data.** `sn_admin_top_tabs()` (inc/admin-tabs-data.php): 8 top tabs, 34 static leaves plus up to 2 spliced (Machine Readers, Search Console), each leaf `{ label, render: callable, wide | wide_card, sub_sections? }`. The dispatcher `sn_admin_render_active_tab( $tab, $sub )` (inc/admin-render-sections.php) prints the sub-tab strip, the wrapper (capped `.sn-fieldset` card / bare `.sn-section` for `wide` / `.sn-fieldset--wide` for `wide_card`) and the leaf. `sn_admin_canonical_destination()` and `sn_admin_post_redirect_target()` are pure.
3. **Every write is one pipeline.** A leaf's `<form method="post">` carries `sn_action` + `_wpnonce` (`sn_theme_options_nonce`); `sn_handle_admin_post()` (admin_init) checks `manage_options`, the page slug, the nonce, looks the action up in `sn_admin_post_handlers()`, calls `handler( $_POST )`, gets a FLASH CODE back, and redirects to the tab/sub (+ `#sn-sec-*` anchor) with `?sn_flash=`; the page maps the code through `sn_admin_flash_to_notice()` into `<div class="notice notice-{severity}">`. **57 forms across 35 leaves, none uploads a file** (the map: `files=false` everywhere). One form (`audit_prune_now`) is handled inline inside its render function from `$_POST`. One GET door (`admin-post.php?action=sn_force_update_check`) redirects to `update-core.php`. Analytics has a POST CSV/JSON export form that streams a file.
4. **The framework's contract** (desktop-mode 1.1.6, `includes/framework`): `->view( callable( State, Os ) )` captures echoed HTML (`View::capture`: `ob_start`, echo + return); `->action( name, handler( State, Os, $args ) )`; markup dispatches with `os-action="<name>"` and carries args as `os-arg-<key>` attributes; a `<form os-action="x">` submit ships every field as `$args['values']` (FormData; multi-values as arrays; the runtime `preventDefault`s submit); a click on an element with `os-action` is NOT `preventDefault`ed, so a rewritten link must lose its `href`; painted HTML lands by `innerHTML`, so inline `<script>` blocks do not execute; effects: `toast`, `open_url( url, title, icon )` (an admin-URL window), `open( id )`, `badge`, `menu`, `refresh_menu`, `announce`, `title`, `close`; `$os->param()` reads open-time params and the `reopen` lifecycle retargets a live window. `->tab()` bakes framework-level views at definition time and cannot be switched by a server action.
5. **Assets are gated on the classic hook suffixes** (`sn_admin_page_hooks()` in inc/admin-menu.php): `sn-admin` (admin.css + admin.js: sub_sections tabs, dirty-tracking), `snt-analytics-tokens`, `sn-analytics-admin`, `snt-confirm`, `sn-analytics-brush`, `sn-resume-admin`, `sn-freshness-dot`, `snt-health-suggest-actions` (three leaves), uptime-status (webhooks); the analytics page's own chart/brush scripts. All self-gate on DOM markers. They never load on the desktop page unless the host appends them. The S&N app already appends handles through `openstation_app_window_args` (inc/openstation-app.php) when the framework cannot map a symlinked path.
6. **The dock today:** inc/desktop-mode-dock.php registers `sn-dashboard` as a URL dock item (the classic page in the shell's admin window) with an 8-tab submenu and a deploy badge, and `snt_desktop_admin_url()` builds admin URLs that the S&N app's doors and the Attention rows open through `open_url`.

## Decisions

| Question | Decision | Why |
| --- | --- | --- |
| One window or two? | Two apps: `sn-dashboard` (id = today's dock id) and `sn-analytics`. | They are two surfaces today with two menus, two capabilities' worth of intent and two vocabularies of state; one window would invent a merge. |
| Framework `->tab()` or state? | ONE `->view()` per app; `tab` and `sub` (Dashboard) / the URL params (Analytics) live in STATE; the host paints its own tab strip and sub-tab strip, derived from the registry on every paint. | A leaf link (the Dashboard's Health card → monitoring/health) must switch the tab from a server action, which framework tabs cannot do; and the registry is resolved at render time (trap 1 of the port memory). |
| How is a leaf painted? | `ob_start(); sn_admin_render_active_tab( $tab, $sub ); ob_get_clean()` (Dashboard) and `snt_analytics_render_dashboard()` with `$_GET` set from state for the duration of the capture, then restored (Analytics). Byte-for-byte the classic body, wrapper and sub-tab strip included. | Zero leaf rewrites; every future leaf lands in the window for free. |
| What is rewritten? | One pass over the captured HTML with `WP_HTML_Tag_Processor`: (a) `<form method="post">` → `os-action="post"`, `action` dropped, `method` kept; (b) `<form method="get">` (Analytics' custom dates) → `os-action="go"`; (c) `<a href>` into OUR pages (`page=sn-theme-options`, `page=sn-analytics`) → `os-action="go"` with `os-arg-*` from the query (`tab`, `sub`, `anchor` from the fragment; every `sn_*` param), `href` REMOVED; (d) `<a href>` to any other admin URL (`update-core.php`, `admin-post.php?action=…`, `post.php`, …) → `os-action="door"` + `os-arg-url`, `href` removed (opens as a shell admin window through `open_url`); (e) external `http(s)` links keep their href and GAIN `target="_blank" rel="noopener noreferrer"` when they lack a target; (f) `<script>` blocks gain `data-snt-exec`. Nothing else is touched. | Only the seams that would navigate the top document or never run are changed; everything a reader sees is the same. (e) is the one recorded deviation: today a targetless external link replaces the admin tab; in a window it would replace the whole desktop. |
| How does a form save? | Action `post` with `$args['values']`: require `manage_options`; require `wp_verify_nonce( values['_wpnonce'], 'sn_theme_options_nonce' )`; set `$_POST = wp_slash( values )` and `$_REQUEST['page']` = the tab's slug (handlers `wp_unslash()` and some read `$_REQUEST`); look `values['sn_action']` up in `sn_admin_post_handlers()` — unknown → nothing runs, one toast "Nothing was saved."; call the handler; take the flash code; apply `sn_admin_post_redirect_target()` to state (tab, sub, anchor); map the code through `sn_admin_flash_to_notice()` into state `notice` = `[ severity, html ]`, painted in the window as the same `<div class="notice notice-{severity} is-dismissible">` above the tab strip, and ALSO `$os->toast( wp_strip_all_tags( html ) )`; restore `$_POST`/`$_REQUEST`. The inline-handled form (`audit_prune_now`) works because the leaf re-renders with `$_POST` populated. | The classic pipeline minus the two things a window cannot do (`header()` + `exit`), reproduced exactly: capability, nonce, page, handler table, flash table, redirect target, anchor. |
| Things a window cannot replay | The `sn_force_update_check` door → `open_url` to its own URL (the destination `update-core.php` opens as an admin window — the same screen the classic link lands on). Analytics' export form → stays a real `<form method="post" target="_blank">` to the classic page URL (a download must be a navigation; a new tab is the least a window can do). Both recorded as deviations; both pinned. | Faithful means the destination still happens, not that the mechanism is identical. |
| Scripts | The window-args seam appends, for `sn-dashboard`: `sn-admin`, `snt-analytics-tokens`, `sn-analytics-admin`, `snt-confirm`, `sn-analytics-brush`, `sn-resume-admin`, `sn-freshness-dot`, `snt-health-suggest-actions`, the uptime-status handles — registered (with their localized data, taken from the same functions inc/admin-menu.php uses) when absent; plus ONE host script `assets/os-host.js` that, on every paint of the window root (MutationObserver, debounced to the paint), (1) re-creates `script[data-snt-exec]` nodes once each so inline scripts run, (2) calls `window.snAdmin.init( root )` — an idempotent seam added to assets/admin.js (its sub_sections tabs and dirty-tracking currently bind on DOMContentLoaded, which fires once). For `sn-analytics`: the analytics page's own handles the same way. | Every leaf behaviour is a script that self-gates on a DOM marker; the host's job is only to make sure each script is present and re-armed after a paint. |
| Dock | The manual `sn-dashboard` dock item in inc/desktop-mode-dock.php is REMOVED (the app registers its own dock entry under the same id, same icon, same title); its 8-tab submenu becomes the window's `menu` (`$os->menu()` at mount and `refresh_menu`), each entry a `go`; its badge (`snt_desktop_dock_badge()`) becomes `$os->badge()` at mount and after every action, from the SAME function. `snt_desktop_admin_url()` is unchanged: the classic page stays reachable, and every door that exists today still opens what it opened. Deep links into the host: `params { tab, sub }` on open and on `reopen`. | One surface per id; the doors keep their contract; the classic page is not removed (a removal is not a port). |
| Sub-sections and anchors | `#sn-sec-*` anchors stay real anchors: the composite Identity & SEO leaf's in-page tabs are admin.js's, re-armed by the host script. A post-save anchor from `sn_admin_post_redirect_target()` becomes state `anchor`, painted as `data-snt-anchor` on the root; the host script scrolls to it once after the paint. | The classic page scrolls by fragment; a window scrolls by script. Same landing. |
| Capability | `->capabilities( 'manage_options' )` on both apps AND the `post`/`go`/`door` handlers re-check `current_user_can( 'manage_options' )`. | The classic pipeline re-checks on every POST; the window does too. |
| Analytics state | State = the page's whitelisted params exactly: `view` (default overview), `range`, `from`, `to`, `class`, `compare`, `drill`, `event_prop`, `lg_range`; `go` applies args through the page's OWN resolvers (`snt_analytics_resolve_view/window/class/compare`) and, on a view switch, resets exactly `snt_analytics_view_reset_params()` (never `compare`). | The URL was the state; the window keeps the same state with the same validators, so a bookmarkable classic URL and a window state are the same fact. |
| What is NOT ported | Nothing is removed from the classic pages; nothing is added to the windows beyond the chrome they must have. The daily-surface client rebuilds are a later, named program. The `signal-noise` app (Notes, Pages, …) is untouched. | Port = faithful. |

## Out of scope

Redesign of any leaf; client-view rebuilds; merging the two windows or the S&N app; retiring the classic admin pages; the S&N app's doors switching from admin URLs to window params (a follow-up once both hosts exist); the Station Home widgets.

## Verification (both hosts)

- Suites: registry parity (the host's tabs and sub-tabs are `sn_admin_top_tabs()` — derived, and a mutation that hardcodes one goes red); the rewrite pass over a fixture holding every case (post form, get form, our-page link with tab/sub/fragment, other-admin link, external link with and without target, inline script, a link that must NOT change); the `post` replay (capability, nonce, unknown action, slashing, handler called, flash → notice + toast, redirect target and anchor applied, `$_POST` restored); `go` validation against the registry and the canonical-destination map; the window-args filter appending every handle once; `$_GET` set-and-restored around the analytics capture; the analytics `go` resolvers and reset list (source-parity with `snt_analytics_view_reset_params()`); the dock item gone and the badge from the same function.
- Sandbox: open the window; visit every tab and every sub-tab (35) and every analytics view (13) — each paints without a PHP notice and with the classic wrapper classes present; save the Now page and read the notice; Full Reset's `data-snt-confirm` still confirms; a Dashboard card link switches the tab; the Links tab's external links open a new tab; the `sn_force_update_check` door opens an admin window; the Identity & SEO section tabs switch; a `#sn-sec-*` landing after a save scrolls; the dock shows one S&N Dashboard with the badge and the 8-entry menu; the phone band paints (stacked chrome is the shell's).

## Amendments

(Recorded by the builds.)

### Host one (2026-09-06)

- **The admin library rides the dispatch.** A window's dispatch is a REST request and loads none of `wp-admin/includes/`; Integrity → MCP Clients answered 500 "Call to undefined function submit_button()" while the same leaf captured cleanly under WP-CLI (which loads it). `snt_os_host_capture()` now requires `wp-admin/includes/admin.php` once when `submit_button()` is absent, the precedent being admin-ajax.php, which does the same. `get_current_screen()` still answers null under REST; the one leaf that reads it guards.
- **The submitter.** The runtime ships `new FormData( form )`, which never includes the clicked submit button; 45 of the 57 forms carry `sn_action` on the button. The rewrite marks named submit buttons `data-snt-submit`, and `assets/os-host.js` carries the submitter (`SubmitEvent.submitter`, the remembered click, or the form's default button) into the form as a hidden input appended last, in a capture-phase document listener that runs before the runtime serialises.
- **Three leaf-owned assets** (Cron's script and strings, Provenance's sheet and stepper, the audit-log sheet) were registered by their own `admin_enqueue_scripts` callbacks behind the classic hook suffixes; each now exposes a registrar the host calls.
- **The window menu.** `$os->menu()` is a context menu at the pointer and `refresh_menu()` rebuilds the shell's registries, so the eight tab shortcuts are `window_action()` declarations instead; `refresh_menu()` runs after a save only.
- State carries `params` (the `sn_*` query params that are state on the classic page: the Tags merge preview) and `flash` (the code, from which the Webhooks leaf derives `?new_id=` once). The notice paints where the classic page paints it (after the subtitle, before the tab strip). `data-snt-anchor` carries the element id (`sn-sec-<slug>`).

The S&N Dashboard host (#1074) was built by three parallel builders against
this spec. Two deviations are already binding here and are recorded in the
CHANGELOG's `[Unreleased]` block: an external link with no `target` gains
`target="_blank" rel="noopener noreferrer"` (in the classic page such a link
replaces the admin tab; in a window it would replace the whole desktop), and
the `sn_force_update_check` GET door opens `update-core.php` as an admin
window through `open_url` rather than navigating to it. Any further deviation
a builder had to take -- a place where the code turned out to be the contract
and this document was wrong -- is appended below by the orchestrator after the
build, in the builder's own words.
**Review fold (four Opus lenses, two refuters each: 29 findings, 28 survived, 21 distinct, all applied).**

- **Three more write pipelines.** The spec's "one pipeline" was wrong for eight forms: Provenance's four `admin-post.php` forms (`sn_prov_reanchor`, `sn_prov_runsweep`, `sn_prov_chain_backfill`, `sn_prov_stage_key` / `sn_prov_rotate_key`), RSS's three (`sn_rss_action`, nonce `SN_RSS_TRACKER_NONCE`, handler `sn_rss_tracker_handle_form()` on `admin_init`) and the inline `audit_prune_now`. The replay dispatches by pipeline; the admin-post and RSS handlers run under a redirect interceptor (a `wp_redirect` filter that throws with the location, the `wp_die` handler filters that throw with the message), so their `exit` never runs and their landing URL becomes the window's state, flash and params; the inline form re-renders its leaf once with `$_POST` populated. A refusal names what was measured.
- **Brackets.** The runtime's values are literal field names; PHP's request parsing never ran. `snt_os_host_expand()` rebuilds it (rawurlencoded pairs through `parse_str()`) before `$_POST` and before the window's params; a repeated name is later-wins; the go-params fixture is the wire shape.
- **Own pages** are every slug the page answers to (top-tab slugs and legacy slugs, filtered by the POST allowlist; `sn-analytics` stays a door), derived on every call; a legacy `page=` link becomes `go` through the same map the 301 uses.
- **Assets:** Machine Readers' sheet and the admin heartbeat script were missing; registrars added like the other three. **rel** is merged. **Any fragment** is carried as the element id (state `anchor` is an id everywhere). **`_wpnonce`** rides an own-page GET link (the worker recheck). **The bootstrap** runs before the superglobals are swapped.
- **Client:** the submitter seam disables a same-named field before carrying the button (a repeated name serialises to an array, not later-wins); admin.js binds once by WeakSet and re-applies the attribute STATE the morph strips, because the runtime paints by MORPH (positional match, attribute sync that removes any attribute the server did not paint); `os-host.js` dispatches `snt:paint` after each pass and five leaf scripts (cron, uptime, the provenance stepper, freshness, brush) re-arm on it -- the other four are document-delegated and need no seam. Three of the five mark by a self-clearing attribute rather than a WeakSet, deliberately: they paint into a mount the server also paints, and a permanent marker would leave the server's placeholder on screen after the first save.
- **CI** fetches WordPress's HTML API (seven files from the core mirror at 7.1) into `$RUNNER_TEMP` for both sweep lanes, and tests/openstation-host.php fails under `CI` when the API is absent, so the rewrite pins can never skip green there.
- Not applied: one finding (that the analytics clamp's parse-time restore is unrecorded) was refuted; it is recorded here: the collapsible analytics panels' saved state does not restore inside a window; the panels themselves work.
- **One request does what two requests did.** On the classic page a save is followed by a redirect, so the leaf that paints next runs in a new request; in a window the replay and the repaint share one. `sn_setting()` memoises the merged settings per request and the save never resets it, so the Identity & SEO save persisted `social_same_as[]` while the same response painted the field empty (measured; a fresh paint showed the value). After any successful write the host now calls the resetters the estate exposes (`sn_setting_reset_cache()`, `snt_ai_reset_availability_cache()`) and fires `snt_os_host_wrote` for any other owner of a request memo.

### Host two (2026-09-06)

- **The export form is the deviation.** The spec already booked it ("Analytics'
  export form → stays a real `<form method="post" target="_blank">` to the
  classic page URL"), and the build confirmed it is the only shape that
  works: `sn_handle_analytics_export()` sets `Content-Disposition`, echoes a
  raw CSV or JSON body and `exit()`s, so it renders no HTML for
  `View::capture()` to capture and the framework has no effect that hands the
  browser a file. `snt_os_host_rewrite()` therefore grew a keep-list — a form
  the host marks keeps `method`, keeps its `action` on the classic page URL,
  and gains `target="_blank"`. The nonce, the handler and the file are the
  page's own; only the tab is new.
- **The dock.** Unlike the Dashboard's, the Analytics tile was never a manual
  registration — the shell auto-imports `add_menu_page()` entries, and the
  `dock_placement` filter in `inc/desktop-mode-dock.php` had only ever hidden
  `sn-theme-options`. With the app registering `sn-analytics` itself, that
  filter hides the menu too, or one id names two tiles.
- **Two limitations carried forward, not re-litigated.** The analytics panels'
  saved collapse state does not restore inside a window (host one's fold
  measured it; the toggles work, the restore pass runs once over `document`
  at parse time), and a cross-host `go` remains out of scope: `sn-analytics`
  is a door from host one and `sn-theme-options` is a door from host two,
  until the doors program.

The builders' own deviations — the places where the code turned out to be the
contract and this document was wrong — are appended below by the orchestrator
after the build, in the builders' words, as they were for host one.

Builders' deviations, in their own words:

- **The request URI is lent too.** `snt_analytics_render_view_tabs()`, the controls and login defense's pill row build their hrefs with `add_query_arg( array() )` / `remove_query_arg()` on `$_SERVER['REQUEST_URI']`; a dispatch's is the REST route, so lending only `$_GET` left every tab pointing at `/wp-json/…` (which the rewrite rightly read as external). The view lends a REQUEST_URI built from `snt_analytics_page_url()` around the capture and restores it in a finally; pinned six ways.
- **Own pages = `sn-analytics` only.** The plan contradicted itself; the view only ever paints the Analytics report, so a `go` for `sn-theme-options` would paint it under a link that said Settings. The gate's "Configure analytics" and the Measurement → Analytics links are doors, as host one treats a link to `sn-analytics`.
- **The export stays a real form** (`sn_handle_analytics_export()` sends `Content-Disposition`, echoes the body and exits): `snt_os_host_keep_forms()` marks it `data-snt-keep-form="<url>"` and the rewrite leaves it real with `target="_blank"` and `action` = the classic page URL. `post` is a refusal seam that names the action if such a form ever reaches it; the page has no other write.
- **The brush no longer navigates inside a window.** Its pointer-up called `window.location.assign` (which would replace the whole desktop); inside `.os-app[data-os-app]` it now dispatches the same `go` the range pills dispatch through a hidden carrier the runtime's delegated click reads. The classic page's path is unchanged.
- `snt_os_host_asset_handles()` takes the host id and answers per host; the window-args seam answers for both ids; `sn-analytics` joins the dock's hidden list so one tile stands for the surface.
- **The framework types the state.** `State::accept()` coerces every write onto the declared default's type and falls back to the default when the shapes disagree; `range` was declared as the integer 7, so `custom` and the seven calendar presets silently became 7 (measured in the sandbox on the custom-date form; the suites' State stubs did not coerce, so the pins were blind). The default is a string now, and the three host suites' State stubs mirror the framework's rule, so a typed default can never hide a string param again.
- **Review fold (four Opus lenses, two refuters each: 10 findings, 8 survived, 3 distinct, all applied).** (1) A NAVIGATION IS THE WHOLE NEXT URL: the first build merged a `go`'s args over the current state ("absent keeps the value"), so the three controls that reset by OMITTING their param -- the Compare Off pill, Clear drill-down, the Events property Clear -- and the movers' bare deep link were dead in the window, and the already-active tab did not clear a drill. `snt_os_analytics_apply()` now applies every navigation wholesale: a param absent from the args takes the page's default, exactly as the classic dispatcher reads an absent `$_GET` key; the brush, which merges into `location.href` on the classic page, reads the current navigation the view paints on the wrap (`data-snt-query`) and ships the whole of it. (2) The lent REQUEST_URI's values are `rawurlencode()`d: `add_query_arg()` builds with encoding off, so a state value carrying `&` would have injected a parameter into every link the report prints; the suite's `add_query_arg` stub now mirrors that (it encoded, and hid the hole). (3) The CI skip guard counted into the summary line: tests/run.sh discards a suite's exit status and reads only "N passed, M failed", so under `CI` skipped rewrite pins are added to the failed count before the line is printed.
- The docs for this host were written before its code landed (the two builders ran in parallel); the mechanism names above are the shipped ones.

### Live (2026-09-06, v13.105.1)

The owner opened both windows on live and they were not faithful, in two ways the sandbox pass had not looked at because it read the DOM and never the pixels: (1) the S&N Dashboard opened from the desktop icon was the classic page in a chromeless frame with NO tab strip -- `admin.css` had hidden `.sn-nav-tabs` under the chromeless body class since v9.62.3 for the shell's own strip, which the shell builds from a dock item's submenu that v13.104.0 retired; (2) the Analytics host painted the classic page on the shell's dark palette -- the app root sets the desktop theme's tokens and colour scheme, and the classic pages are light-only, so white cards carried light text. Fixed: the hide rule is gone (the page's own strip is the navigation in every window); both host roots declare wp-admin's canvas as their own tokens and remap the shell's, per the shell's own rule for an admin page in a window; the desktop icon targets the host window by id. The verification list now says: a screenshot of each window under the dark palette, read by eye, beside the classic page.

A third report followed the same afternoon: Analytics, which the owner had moved to the desktop before the update, sat back in the dock after it. The shell keys a user's placement (`navPlacement`, `navOrder`, `mobileTabs`, `dockPromotedPositions` in the per-user `desktop_mode_os_settings` meta) by nav id, and an auto-imported menu tile's id is the menu's hook name, `toplevel_page_sn-analytics`, while the app entry that replaced it is `sn-analytics`; the preference named an item nothing paints any more, and the app entry had none. Measured in the sandbox: the shell honours `navPlacement['sn-analytics']` the moment it exists. The carry is a one-time sweep on `admin_init` at priority 20 (`inc/desktop-mode-nav-ids.php`, behind the option `snt_os_nav_id_migration`) that copies each preference from the old id onto the app id for every user who has the shell's meta, through the shell's own getter and saver, never overwriting a value set on the app id since, and keeps the old key so the menu tile is where it was if the apps ever go. Verified end to end with the owner's exact meta on user 1 and the auto-import hidden as on live: one reload, and the Analytics tile is on the desktop at its saved position, once, and out of the dock. The lesson for the port program: replacing a surface under a NEW id orphans every per-user preference keyed by the old one; a port that keeps the id (the dock entry did, `sn-dashboard`) needs no carry, and one that cannot must ship one.
