# Changelog

All notable changes to Signal & Noise Tools are documented here.

This file holds two things only: **`## [Unreleased]`**, the working log that
accumulates across pull requests, and the **current release**. Everything older
lives in [docs/changelog/](docs/changelog/).

A pull request does not bump `Version` and does not tag — it closes an issue and
adds a bullet below. A release is a separate, deliberate act:
`tools/cut-release.sh`.

## [Unreleased]

## [17.6.0] - 2026-09-21 — the window is built from the kit

### Added
- **Every relative time in the S&N window ages live through `<os-relative-time>`.** Read against 17.5.1: 25 moment sites in 14 leaf files under `apps/sn-dashboard/parts/leaves/` painted `human_time_diff()` once as a static string, frozen at paint in a long-open window or the PWA shell, and the Site pulse "Updated 3:14 pm" stamp was a `current_time()` clock reading; the cron ledger's next run routed through `snt_cron_next_run_label()` to reverse the unsigned wording into "5 mins overdue". OpenStation's `<os-relative-time datetime>` (Stable at 1.1.10) formats through `Intl.RelativeTimeFormat`, is signed, ticks every 30 s on one shared timer and paints a native `<time title>` with the absolute moment. Changed: one kit helper, `snt_kit_relative_time( $timestamp_gmt, $fallback_text = '' )` in `inc/openstation-kit-display.php`, paints the element with an ISO 8601 UTC `datetime` and the server's own signed `human_time_diff()` reading ("5 mins ago" / "in 5 mins") as its light-DOM fallback, so the paint reads the same before the component upgrades and in the fixture suites; 25 sites move onto it (23 of the issue's 25, the Updated stamp and the Firewall row), the sentence templates drop their own "ago" ("Checked %s." holds the element), the cron leaf's overdue branch collapses into the signed fallback (the classic renderer and the health line keep `snt_cron_next_run_label()`), the Continue working rows anchor on `post_modified_gmt`, the Site pulse stamp is the element on the paint moment with "just now" as its fallback, and Security › Firewall's last-unpinned row comes along. Two sites cannot hold an element from PHP and stay as they were: the Cloudflare probe table's When column (`os-table` cells are JSON data) and the write-door credential select's "created %s ago" (an `<option>` label). The cron interval duration ("hourly (1 hour)") is a duration, not a moment, and stays. Classic pages are untouched. Pinned: the helper (ISO UTC attribute, "%s ago" and "in %s" fallbacks, a caller fallback escaped, the timestamp cast before it becomes the attribute) and the Continue working row plus the Updated stamp in `tests/dashboard-home-pulse-and-work.php`; a past next run painting the element with "1 hour ago" and neither "overdue" nor "in 1 hour" in `tests/os-leaf-connections-cron.php`; and the nine leaf suites' "ago" pins moved onto the element with the fallback inside; 20 red against 17.5.1 with the leaves and the helper swapped back, before the dashboard suite fatals on the missing helper with 6 more that cannot run there. #1596.

### Fixed
- **The rows and tiles in the S&N window are the kit's layout primitives.** Read against 17.5.1 and OpenStation 1.1.10: the window painted its rows with four house classes and styled them by hand in two sheets, `.snt-cols` (a fixed pair, 38 hits across 23 leaves and `snt_pair_cards()`), `.snt-2up` with `.snt-2up-col` (a second pair whose flex column also carried a sibling margin, so boxes inside one column sat 36px apart), `.snt-stats` and `.snt-systems` (auto-fit tile rows, the latter with an outside restyle of the `<os-stat>` host), and `.snt-col` (a bordered cell); each needed a container query, a width-cap hatch keyed on its name, a section-rhythm reset and a form release to hold, and two rules (`.snt-card`, `.snt-dashboard-home`) matched no markup at all. Changed: one helper, `snt_kit_grid( $items, $min_item_width = 290, $gap = 18 )`, paints `<os-grid min-item-width gap>` (Stable at 1.1.10: as many equal columns as the grid's own width fits, one below the minimum, no container query), drops a side that painted nothing and returns a lone box bare so it stands at full width; 290 reads the 640px container query it replaces (a 640px window leaves the leaf 602px inside the body inset and 2 * 290 + 18 fits, 636px does not), tiles ride at 160/10 and the systems wall at 190/12; `snt_kit_stack()` paints the `<os-stack gap="18">` column the two-up leaves use, so the 36px inside a column closes to 18; `snt_kit_section()` passes `stack`, so the body is the kit's flex column and `::part( body )` keeps only the box and a 16px `--os-ui-section-gap`; the `.snt-col` cells are `<os-card>`, the landmarks on the Music row, the Performance rail and the Provenance rail ride as `role="complementary"` on the kit element; the stat tile is styled through `--os-ui-stat-padding`, `-radius`, `-border` and `-bg` as the Analytics window already does; the leaf width-cap hatch and the form release key on `:has( os-grid )`, the rhythm reset covers an os-grid, os-stack or os-card parent (three selectors, since the cap suite's scanner splits on commas), and the two dead rules, the four container queries and the outside `os-stat` restyle are gone. One thing the kit does not do and the port had to: `min-item-width` fits the tracks to the width alone and keeps the empty ones (`updateTracks()`: `repeat( columns, minmax( 0, 1fr ) )`), which is the auto-fill shape `tests/os-grid-auto-fit.php` measured on 2026-09-10 (four tiles in eight 202px tracks), so the helper writes the item count as `--snt-grid-items` and one rule in `assets/os-app.css` caps the tracks at `min( the kit's tracks, the count )`; a pair stays a pair on a released 1,700px leaf and a four-tile row fills it. The compat stamp moves to `v1.1.10` after the doc's name sweep against the tag (every seam present). Pinned in `tests/os-layout-primitives.php` (no leaf, analytics part or kit helper paints a retired class, comments stripped; neither sheet carries a rule for one, the dead rules, an outside `os-stat` padding or border, or a container query for a row; the helpers by shape and by output), the leaf harness's classic-marker list gains the five retired classes, the cap suite reads the `os-grid` hatch and the three resets, and the auto-fit suite pins the track cap; 4 red in the pin, 5 in the RSS suite and 1 in the auto-fit suite with 17.5.1's `os-app.css` and one leaf swapped back, 15 in the pin against 17.5.1 whole before it fatals on the missing helper. #1622.
- **The S&N window's body is the kit's app frame.** Read against 17.5.1 and OpenStation 1.1.10: `tab_view()` in `apps/sn-dashboard/parts/frame.php` painted the leaf bar and then a `.snt-dashboard-body` div, a hand-rolled scroller spread over two sheets (the flex column, the inset, the gap, `overflow: auto`, the stable gutter, the Home tab zeroing it and turning it to `overflow: hidden`, the phone inset, the PWA safe-area bottom), with the 820px leaf cap and its hatches keyed on `.snt-dashboard-body >`. `<os-app-frame>` (Stable at 1.1.10) is that shape: a grid of auto / auto / minmax( 0, 1fr ) / auto rows, the toolbar slot staying put, the default slot scrolling, `::part( content )` exposed for app styling and `contained` for a body whose children own their scrolling. Changed: the view paints `<os-app-frame>` (`contained` on S&N Home, whose rail and main scroll on their own) with the 17.4.3 leaf bar in `slot="toolbar"` (`snt_kit_tabs()` takes attributes) and the notice and the leaf in the default slot; the inset, the 12px gap, the stable gutter, the Home zero inset, the phone inset and the safe-area bottom sit on `os-app-frame::part( content )`, the Home tab's `overflow: hidden` is the `contained` attribute and its notice is pinned to `flex: 0 0 auto` so it does not stretch; the cap and every hatch read `os-app-frame > .snt-leaf` (0,3,1), each hatch still strictly outranking it; the root flex column in both sheets stays because the frame needs a definite height from its parent; no `<os-body>` inside the frame (its host is `height: 100%`, so a tall leaf would spill past its bottom padding, and with `scroll` there would be two scrollers); the runtime upgrades the tag from the paint, so no `loadComponents` call. The poll and anchor effects (#1607, #1609), the `snt-dashboard` container query and the phone rules are untouched. Measured in `tests/js/window-geometry.mjs` against the 1.1.10 shell bundle at all 26 window sizes: the `scroll` and `initialReport` boxes of Connections > IndexNow, Site > Performance and S&N Home are identical before and after to the pixel, 0 failures both sides, once the Home fixture's `WP_Query` stub gained the `found_posts` the painter reads (its warning text had been landing inside the leaf as an 18px line). Pinned: `tab_view( 'monitoring' )` paints `<os-app-frame>`, the toolbar with `slot="toolbar"` inside it above the leaf and no `snt-dashboard-body`; `tab_view( 'dashboard' )` paints `<os-app-frame contained>`; the sheet carries `os-app-frame::part( content )` with `scrollbar-gutter: stable` and no `.snt-dashboard-body` selector; the cap suite reads the cap at (0,3,1) on `os-app-frame > .snt-leaf` and the type hatches carrying that shape; the two fixtures paint the frame and the geometry harness measures the frame's content part through its shadow root after `customElements.whenDefined( 'os-app-frame' )`, with the frame's last light-DOM child as the end it must reach; 2 + 2 + 5 red with the view and the sheet swapped back. #1617.

