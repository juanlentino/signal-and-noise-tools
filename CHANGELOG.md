# Changelog

All notable changes to Signal & Noise Tools are documented here.

This file holds two things only: **`## [Unreleased]`**, the working log that
accumulates across pull requests, and the **current release**. Everything older
lives in [docs/changelog/](docs/changelog/).

A pull request does not bump `Version` and does not tag — it closes an issue and
adds a bullet below. A release is a separate, deliberate act:
`tools/cut-release.sh`.

## [Unreleased]

### Added
- **Reschedule is a bulk action in the native Posts window.** The classic posts list has carried "Reschedule (Signal & Noise)" since 13.56.0, a bulk action with a datetime-local field in its toolbar and a planner that refuses the two boundaries core coerces at (a scheduled post moved to within a minute of now publishes; a published post moved a minute ahead unpublishes). OpenStation's Posts window has no bulk dropdown and no toolbar field, so from the window the owner could not move a batch at all; the native-twin audit found the seam the shell offers, `openstation.postsWindow.bulkActions`, unconsumed. Now: one entry, "Reschedule…", on that filter (`assets/os-posts-reschedule.js`, enqueued only for `edit_others_posts`); the date is asked in the shell's `<os-modal>` holding a native datetime-local input labelled as site time (the shell's confirm is yes/no only); Cancel keeps the selection; the write is `POST signal-noise/v1/openstation/reschedule` (`inc/batch-schedule-rest.php`), behind the classic action's `edit_others_posts`, sent through `wp.os.fetch`, the shell's own helper that stamps its REST nonce at call time and refreshes it on every heartbeat tick (a nonce localized at page load is the wrong one once the shell, a PWA on the desktop or the phone, has sat open past the nonce window; the Attention pill's read carried exactly that since it shipped and goes through the same helper now, the localized nonce is gone from the object; a 403 toasts a reload hint; a request that never reaches the site toasts as such and keeps the selection, since the shell's runner would otherwise clear it on a rejection with no verdict); the answer is the classic notice's sentence, both halves, as a toast, and the window repaints. The write itself moved into one shared `snt_batch_schedule_apply()` that both surfaces call, so the two cannot drift, and it now checks `edit_post` per id and skips anything that is not a post (a duplicated id counts once); this tightens the classic path too, which checked only the batch-level capability before, so the skipped count is a fourth sentence in the shared message and a fourth query arg, `snt_batch_skipped`, on the classic redirect: a batch of nothing but skipped rows says why instead of "0 posts rescheduled.". The filter has no mode argument, so the button also paints in the Pages window: `run()` refuses there with "Reschedule is for posts only" and the route would skip pages regardless. Pinned: the route and its gate by name (not `manage_options`), the wire value `YYYY-MM-DDTHH:MM` parsing through the same shape check as the classic field, a bad date as a 400 with the classic sentence and no write, refused and skipped rows reported on both surfaces never dropped, a page and an uneditable id skipped never written, the classic handler answering the three classic query args and the skipped count, the script gated on the capability, no `wp.os` at load, both scripts on `wp.os.fetch` with no hand-set nonce header and no localized nonce, the sentences word for word; red against the unfixed code. Native-twin audit 2026-09-20, gap #2 of eleven.
- **The morning brief and the scheduled read-only runs can be set from Connections › Cron in the window.** The native leaf painted the events table alone, and its header said the classic tab had no forms; the tab is the whole `sn_admin_cron_tab` hook, and two of its three callbacks carry a form (`morning_brief_save`, `scheduled_reads_save`), so the owner had to open wp-admin to turn either on, send a test brief, acknowledge settings drift, or run the reads now. Now the two boxes share one row under the ledger (the events table stays at full width) with the same field names and actions: one toggle each, the helper, last-sent and last-run as plain hints, a failed send as a notice, drift as a warn notice on top of the brief box, Save, and each secondary verb (Send test brief, Acknowledge current settings while drift exists, Run now) as its own kit form carrying the differentiator as a hidden field, the login-defense shape; no host change. Part of it: both handlers read the toggle with `isset()`, and a native unchecked box arrives as `''` (`snt_os_host_expand`), so a window Save could switch either toggle on but never off; they read `! empty()` now, the way the digest twin already did. The oracle captured the priority-10 table alone, so its parity pins passed as `[] === []`; it captures the whole hook through `sn_admin_render_cron_section()` now. Pinned: names and actions on both sides with and without drift, the fixture carrying two forms, one `.snt-cols` with two boxes and the table above it, every readout present and absent, the painted OFF reading false through os-form and expanding empty, and the handlers turning each toggle off from that; red against the old leaf, red against the `isset()` read, and the old capture green against the old leaf (the hole). Native-twin audit 2026-09-20, gap #4 of eleven.

### Fixed
- **A breached password was refused on the classic profile screen and accepted from the native one.** Mode A (13.58.0) ran on `user_profile_update_errors` and `validate_password_reset`, the two hooks core's profile form and reset form fire. OpenStation's profile window saves through `PUT wp/v2/users/{id}`, and core's users controller hands the request's `password` to `wp_update_user()` without either hook, so on the owner's own path a breached or uncheckable password reached the hash with no check; the fail-closed promise in the file's header was false there. Now `rest_dispatch_request` runs the same guard over `$request['password']` on a write to the users controller (create, update, update-current) and hands core the refusal as the dispatch result (400, `params.password` naming the field, the same message the classic screen shows; the window maps both). It runs after the route's permission callback, so an unauthenticated request never reaches the breach client, and a read carrying `?password=` is not judged. Counts the rejection like any other; a clean password, no password, another controller, or an earlier filter's result pass through unread. NOT `rest_pre_insert_user`, the obvious seam and the wrong one: core's `update_item()` takes `prepare_item_for_database()`'s return without an `is_wp_error()` check, sets `->ID` on it and writes `(array)` of it, so a `WP_Error` there is a silent 200 that drops every field in the save; the file is pinned off that hook by name. Pinned: the three hooks and nothing else, breached and unreachable both refused at the door, clean, read, foreign-controller and password-less untouched; red without the filter. Found by the native-twin audit of 2026-09-20 (#1 of eleven); the seam by the workflow's core-flow skeptic, against my own review.
- **Suggest runs in the desktop.** The owner could not run an AI fix from the desktop: Content › Block Migrations and Content › Pattern Adoption painted Suggest disabled with a title pointing at the classic page, and Monitoring › Health painted no Suggest at all. The shared script (assets/health-suggest-actions.js) already reached every kit button; its DOM walks were wp-admin-shaped (`closest('td,th')` for the cell, `closest('tr')` for the row, `.sn-fieldset` for Suggest all), and a button with no cell returned in silence. Now one cell selector (`td,th,os-cluster`), one row selector (`tr,os-row,os-card`), one section selector (`.sn-fieldset,.snt-check`), `hasAttribute('disabled')` where `.disabled` read an `<os-button>` host's attribute string as falsy, and a `console.warn` naming the button when no cell is found. Both Content leaves enable Suggest and drop the hint; Pattern Adoption wraps its pair in an `os-cluster`; Block Migrations loses its in-queue Refresh (the title bar has one, and Dismiss removes its own row now). Health paints every check's findings as the Block Migrations row shape (os-row, os-cluster, Edit as a door instead of the URL as text) with the AI fix column and Suggest all on the classic gate (a provider, a check with a Suggest path); the data contract comes from the classic builder, split out as `sn_health_suggest_cell_attrs()` beside `sn_health_suggest_supported_checks()`, output byte-identical. Review pass: an `os-cluster` is a flex host, so the editor the script painted into it took only its intrinsic width (152px of a 286px cell); `os-cluster > .snt-suggest-panel, .snt-verdict-panel { flex: 1 1 100%; min-width: 0 }` fills the cell as the classic `<td>` does. Health's Edit door now blanks a non-http(s) `edit_url` the way the classic cell's `esc_url()` does (the Block Migrations permalink rule). Pinned: Suggest enabled on both Content leaves, the pair in its cluster, no hint, no in-queue Refresh; Health's AI column present with a provider and absent without, the attachment finding's exact contract, the inline_svg finding's empty cluster, Suggest all 2 with the cap, the attributes equal to the classic leaf's, a `javascript:` edit_url blanked, the cluster panel rule in the stylesheet; each pin shown red against the unfixed leaf. Follow-up: the buttons the script mints inside the kit (Discard, Apply, Copy, the modal) are still wp-admin `.button`s. Native-twin audit 2026-09-20, gap #3 of eleven.

## [17.2.1] - 2026-09-20 — boxes share a row

### Fixed
- **Content › Tags is boxes on rows, not a scroll.** The owner opened 17.2.0 to a registry painted as twenty-six selects under a per-tag list of twenty-five, one box after another. Now: three paired rows in the house's two-column grid (duplicates beside the picker that folds them, Jev's tag fit beside the per-tag reading it comes from, the headings beside the unused tags), the glance and the recent list alone; a side that paints nothing leaves the other at full width. "Jev: by tag" is one summary line ("25 tags; 4 carry notes that only touch them: ...") and then only those tags with their notes. "Groups on /notes/tags" is a ledger (one line per heading naming its tags, "Not yet filed" only when a tag is) and ONE small form (a tag, a heading, File); `tag_group_apply` takes it beside the per-tag map. The classic page paints the same ledger and form. A registry is not a form; a reading is not a fold either, the owner said, when boxes can share a row.
  PHPStan reads a `function_exists` guard only in the function that holds it, so the ledger builders carry their own (the theme owns `sn_notes_tag_groups()`); the caller's guard keeps the message.

