# Changelog

All notable changes to Signal & Noise Tools are documented here.

This file holds two things only: **`## [Unreleased]`**, the working log that
accumulates across pull requests, and the **current release**. Everything older
lives in [docs/changelog/](docs/changelog/).

A pull request does not bump `Version` and does not tag — it closes an issue and
adds a bullet below. A release is a separate, deliberate act:
`tools/cut-release.sh`.

## [Unreleased]

### Fixed
- **Integrity > Provenance painted five one-click writes as `<os-form>`s with their header and fields hidden by CSS; the compat doc's shape for a one-click write is `<os-button os-action="post" os-arg-action os-arg-nonce>`.** Read against 17.4.4: `provenance_post_action()` (`apps/sn-dashboard/parts/leaves/tools-provenance.php`) hand-built an `<os-form class="snt-provenance-action">` around two hidden inputs for each of `sn_prov_reanchor`, `sn_prov_runsweep`, `sn_prov_chain_backfill`, `sn_prov_stage_key` and `sn_prov_rotate_key`, and `sn-dashboard.css` hid `::part(header)` and `::part(fields)` and stripped the footer border so the form read as a button. The leaf's own docblock named the cause: `snt_kit_action_button()` always posts its action as `sn_action` because `posted_values()` (`sn-dashboard.os.php`) maps a button's `action` arg to that name, while the host's admin-post pipeline (`inc/openstation-host-pipelines.php`, `snt_os_host_pipeline_for()`) routes on a field literally named `action`. The form existed only to carry that field name. Now `posted_values()` names the field `action` when the button declares `pipeline=admin-post`, the five call sites go through `snt_kit_action_button()` (`os-button`, Stable at OpenStation 1.1.10) with `os-arg-pipeline="admin-post"` and each handler's own nonce (`wp_create_nonce( $action )`, the name every one of the five checks with `check_admin_referer()`), never the shared `sn_theme_options_nonce`, and the `.snt-provenance-action` CSS block is gone rather than hiding chrome. No confirm on any of the five: classic confirms none, re-anchor is withheld once anchored, and the rotation handler's docblock refuses a dialog on purpose. Pinned in `tests/os-leaf-tools-provenance.php` (no `<os-form>` on the leaf, each of the three baseline and two rich-fixture writes a post button carrying its own nonce and the admin-post pipeline, the shared nonce and every hidden `_wpnonce` input absent, the CSS block gone) and `tests/openstation-app-dashboard.php` (`posted_values()` with `pipeline=admin-post` yields `action` + `_wpnonce`); ten pins red against the unfixed files. The M half, `admin-post.php` with a nonce per action for the 56 classic forms behind the shared dispatcher, is not this change. Refs #1614.
- **The host script inferred a paint from three MutationObservers; the native shape is the runtime's own `os-app-effect`.** Read against 17.4.4 and OpenStation 1.1.10 source: `assets/os-host.js` gave every app root a subtree observer, raced a frame against a 50 ms timer behind a latch, and watched `document.body` with an attribute filter just to find roots, so every write the pass made bought one extra no-op pass and every marker had to be chosen for whether the morph would keep or strip it; `assets/os-kit.js` ran a fourth observer on the document that scrolled the same `data-snt-anchor` with `behavior: 'smooth'` and never removed it, so one save could scroll twice. The runtime announces every paint: `$os->effects->add( $type, $data )` queues a custom effect, and after `morphChildren()` and `finishRender()` the runtime re-dispatches any type it does not perform itself as an `os-app-effect` CustomEvent on the app root, bubbling and composed, `detail = { appId, windowId, view, effect }` (`src/app-runtime/session.ts` `performEffect()`, `docs/app-framework.md` Effects; App Framework, Experimental, present since v1.1.6, the verified floor). Every paint the two windows get (mount, the prewarmed mount, a dispatch) runs through the same `apply()`; the one `apply()` fed `effects: []` is `paintEagerly()`, a client-view path neither host uses, so no observer is kept. Both frames now queue `snt-paint` with the anchor from state through a `paint_effect()` helper behind a `method_exists` guard naming the seam, and the dashboard frame stops painting `data-snt-anchor` and clears the anchor from state once queued (the runtime echoes state after render), so a Refresh or an inline post after a save queues an empty one and scrolls nowhere; `os-host.js` replaces `host()`, `scan()` and the two observers with one `onEffect()` listener that finds the `.snt-app` frame at or under the event target and schedules the pass with the effect's anchor, the frame-or-timer deferral kept for `renudgeTables()` and the latch one-shot per effect with a WeakMap carrying the latest anchor; `os-kit.js` drops `scrollToAnchor()` and its observer, keeping the `.snt-go` click carrier. Pinned: `tests/openstation-host-assets.php` Groups 1, 2 and 5 name the listener, `onEffect()` down to its `schedule( root, ... )` call, the absence of `MutationObserver`, `host()`, `scan()` and `data-snt-anchor` in the comment-stripped host, the absence of any observer and scroll in the kit, and the WeakMap; `tests/openstation-host-dashboard.php` Group 7 reads one `snt-paint` effect off the paint with the id unchanged, no attribute in the HTML, the anchor cleared from state and an empty anchor on the next paint of the same session; `tests/openstation-host-analytics.php` Group 10 reads it off the main and a tab view. Eighteen red against 17.4.4. (#1609)
- **The docked cards colour themselves with private literals and the four timer cards poll a hidden tab; the native shape is the widget card token contract and Recipe 2.** Read against 17.4.4 and OpenStation 1.1.10: none of the eleven `assets/desktop-mode-widget*.js` files read a `--os-ui-color-*` token. Muted text was `opacity` at six private levels (65 sites), hairlines a bare `rgba(255,255,255,0.12)` or `0.14` (11 sites), and the 13 link sites plus the views sparkline borrowed `--os-window-link-accent` and `--os-window-link-color`, the overlay lines drawn between related windows: with no theme worn they happened to follow the accent picker, with Legacy worn they stayed the theme's `#2271b1`. OpenStation 1.1.5 declared the card's own contract, on-dark by design (`assets/css/variables.css`, documented under "Theme tokens" in `docs/examples/register-widget.md`, Stable with the widget API): `--os-ui-color-text-subtle` chains to the muted-on-dark Legacy keeps light, `--os-ui-color-border` is the in-card hairline, `--os-ui-color-accent` chains to `--os-ui-accent`, the name the picker writes inline on body, so with no theme worn the links follow the pick. Under Legacy, the one built-in theme, they do not: 1.1.10's `assets/desktop-themes/legacy/theme.json` pins `--os-ui-color-accent` to a literal `#3b82f6` and `--os-ui-color-border` to a light `#e5e7eb`, and `includes/desktop-themes/compile.php` emits both on `.os-shell[data-os-desktop-theme="legacy"]` after `variables.css`, so with Legacy worn the links move from `#2271b1` to `#3b82f6` and still ignore the pick, and every hairline and the Quick Actions button border paint that light grey on the dark glass, the shape upstream's own bundled widgets take under Legacy. A `var()` fallback cannot route around a declared value; that is an upstream defect to file against the theme, and the honest reading of the change is the contract's names with the contract's Legacy values. Every muted style string now reads `color: var(--os-ui-color-text-subtle, rgba(255,255,255,.N))` with the old opacity as the fallback alpha, the three `HAIRLINE` constants and eight inline hairlines read `var(--os-ui-color-border, ...)` with today's literal, and the 14 accent sites read `var(--os-ui-color-accent, #4a9eff)`; a station older than 1.1.5 paints what it paints now. The green, amber and red status glyphs have no widget token and stay literal, the second premise of memory TRAP 8 still holding. Polling: the deploy and uptime cards re-armed a bare `setTimeout` after every call, RSS and the queue ran a bare `setInterval` (plus a `focus` listener on the queue), and the cache card's `visibilitychange` listener called `refresh` unconditionally, so a quick tab flip cost a call; 17.4.2 measured 43,049 of the 50-day first-party calls on the two unguarded ability pollers. All four now follow Recipe 2 from the same doc: the timer chains keep their backoff through a `nextAt` and arm only while visible, re-arming on reveal for what is left of the wait (zero when stale); the intervals stop on hidden, restart on reveal and catch up at once only when `Date.now() - lastRunMs >= REFRESH_MS`; the cache card stamps its last run and refreshes on reveal only when that is older than the poll. The `focus` listener goes, since reveal-when-stale covers it. `docs/openstation-compat.md` (CSS) and the `desktop-mode-integration.php` comment that named the `--wpd-color-*` phantom now carry the 1.1.5 fact. Pinned in `tests/widget-card-tokens.php` on source with comments stripped: no `--os-window-link-` in any widget, no hairline outside a border-token fallback, no `opacity:.N;` in a style string, every `--os-ui-color-*` read with an on-dark fallback, each link file and the sparkline on the accent token, each poller's listener, hidden stop, stale-only catch-up and removal at teardown, the cache gate, and the status literals as the negative control, with the bridge pin (no bare `color:#4a9eff`) carried over from `tests/desktop-widget-token-bridge.php`, which pinned the old borrow by count and retires; `tests/desktop-status-resilience.cjs` gives its document a visibility state and drives the two timer chains through a five-minute hidden stretch (no call), a reveal (one call each), a one-second flip (none) and a resumed period. 61 pins red against the unfixed files. #1603.
- **`sn_settings` had five raw writers and no `register_setting()`; core's `sanitize_callback` on `init` is the shape.** Read: no `register_setting`, `sanitize_option_*` or `pre_update_option*` existed for the option; `sn_settings_save()` sanitised its own posted fields and `sn_setting_update()`, the activation seed, the `admin_init` prune and the Machine Readers save wrote whatever they were handed, as would WP-CLI, a migration or an ability. Changed: `sn_settings_register()` on `init` declares the option in the private `sn_private` group (no `settings_fields()` form exists, so options.php never saves it) with `type` object, `show_in_rest` false (two keyring leaves live in the tree) and `sn_settings_sanitize_option()` as the callback, which core binds to `sanitize_option_sn_settings` so every `update_option()` and `add_option()` runs it; no `default`, since every reader passes `array()` and `sn_setting()` merges the defaults. The sanitiser coerces each leaf to its default's type (strings through `sanitize_textarea_field()` so the seo_copy descriptions keep their newlines, bools, numerics that keep a 7.50 credit), routes `_url` leaves through `esc_url_raw()` and passes `search_console.property` through trimmed, since core's field sanitisers strip every percent-encoded octet and `esc_url_raw()` strips an `sc-domain:` scheme, passes credential-shaped leaves and every key the defaults do not describe through untouched (`identity.job_title`, `machine_readers`, `monitoring`, the GSC credential JSON), and is a fixed point on its own output. No writer's stored value changes. Pinned in `tests/settings-register.php` (the hook is `init`, the group, `show_in_rest`, no `default`, the sanitiser's twenty leaf readings, and a percent-encoded image URL, URL-prefix property and domain property with core's octet loop in the stub; 26 red against 17.4.4), and the ten sibling suites that require `inc/settings.php` standalone gained an `add_action` stub. #1612

- **Three SN maintenance commands reached the shell palette twice; the native shape is one registration per document, gated by the request predicate.** Read against OpenStation v1.1.10 source and docs and measured on the installed station 2026-09-20: Cmd+K, Commands, "purge" listed `SN: Purge all caches` twice. `inc/command-palette.php` enqueued `snt-command-palette` (dep `wp-commands`) on every admin request, so on the shell the `199a0851` hoist replayed it and the shell's harvester (`src/commands/shell-harvester.ts`) republished every `core/commands` entry as `global-signal-noise-*` next to the `sn-cmd-*` registration from `inc/desktop-mode-commands.php`; the harvested copy fed back through a wp-admin `.notice` strip in the shell body, carried no `description` or `aiCallable`, turned the classic navigation commands (`window.location.assign`) into station reboots, and in a block-editor window the iframe bridge made a third, eager copy. Now the `admin_enqueue_scripts` callback returns first thing when `openstation_is_shell_request()` (Stable, "gate shell-only enqueues and output on this") or `openstation_is_chromeless_request()` (`includes/core/routing.php`, not marked Stable) is true, both behind `function_exists`; the shell's copy is the `sn-cmd-*` registration through `wp.os.registerCommand` with the 17.4.4 `wp.os.showToast` feedback, and classic wp-admin outside the station keeps the `core/commands` registration and its notice strip unchanged. The file header and `docs/openstation-compat.md` now say the contributor is gated by predicate, not hoisted. Pinned in `tests/command-palette-localize.php` on the recorded `wp_enqueue_script` handle, not only the localize call (shell and chromeless requests neither enqueue `snt-command-palette` nor localize, a classic request still does both); four red against the unfixed file, two red against a build that keeps the enqueue and only skips the localize. #1626.

### Added
- **The per-post settings rode the legacy meta box bridge; `PluginDocumentSettingPanel` from `@wordpress/editor` is the editor's native panel.** Read against 17.4.4: `inc/post-settings.php` registered `add_meta_box( 'sn_post_settings' )` on posts and pages and painted thirteen raw `<input>` and `<textarea>` fields that the bridge dropped unstyled into the sidebar (`admin.css` never loads on the edit screens), `assets/ai-meta-description.js`, `assets/ai-og-card-title.js` and `assets/ai-excerpt.js` each polled `document.getElementById` for up to ten seconds and appended a button into the box, and the box saved through `save_post` behind a nonce while `assets/pre-publish-gate.js` read `getEditedPostAttribute( 'meta' )`, so a meta description typed into the box and not yet saved was invisible to the gate. Now `assets/post-settings-panel.js` registers the `snt-post-settings` plugin whose render is a `PluginDocumentSettingPanel` (Stable, exported since 6.6) over `useEntityProp( 'postType', type, 'meta' )` from `@wordpress/core-data`, painting the same fourteen fields with the same labels and helpers as `CheckboxControl`, `TextControl` and `TextareaControl`, Pages-only rows (signing, the pillar pair) filtered on the edited type; the three AI scripts push actions onto `window.sntPostSettingsActions` and the panel paints them as `Button`s under their fields, writing a returned value into the entity so the gate sees it before publish. Save semantics: the editor sends the whole meta object on every save, and for a key at its registered default that was never stored, core's `WP_REST_Meta_Fields::update_meta_value()` finds no row to compare against and writes the default as an `''` row (read in core), so the panel's `normalize()` hands every S&N key at its default (`''` or `false`) back as `null` before each entity write, which the controller turns into `delete_post_meta()`, a no-op on an absent row; a flag stores `'1'` or nothing, text the sanitized string or nothing, the shape `abilities-helpers.php`'s existence probe on `_sn_pillar` already keys on. The pillar pair flips to `show_in_rest => true` (the panel is the React sidebar the file said to wait for; `auth_callback` already gated per post), the focus keyword's 80-character cap moves onto its registered sanitizer so both write paths share it, the prepop sentinels register in REST for the panel's "auto-generated when you published" `Notice` (dismiss clears them in the entity and through the `prepop-dismiss` ability), and `rest_after_insert_{post,page}` clears them on an editor save the way the classic handler did. The meta box, its render, its nonce and `sn_post_settings_save` are gone: nothing printed the form fields once the box left, so the handler could never run, and every key's registered `sanitize_callback` is the one write path. The enqueue is gated on the post and page document screens (`get_current_screen()` base `post`, type in the supported pair), the gate the pre-publish gate and the AI scripts keep on `post.php` and `post-new.php`, because `enqueue_block_editor_assets` also fires in the site editor and the widgets editor, where the editor package mounts the panel slot for a template. Not kit-styled: the os-* kit is not in the editor frame, and the panel does not need it. Pinned in `tests/post-settings-panel.php` (no `add_meta_box`, the sentinel and pillar REST registration, the `enqueue_block_editor_assets` registration with its seven deps, the `rest_after_insert` hooks, the script's `registerPlugin( 'snt-post-settings'`, `wp.editor.PluginDocumentSettingPanel`, `useEntityProp`, every label and key, the `null` write shape, `normalize()` over the field table and the sentinels on both entity writes, the panel staying out of the site editor, the widgets editor and an attachment screen, no `save_post` hook and no nonce, the three AI scripts as pushed actions with no `getElementById`), and the render pins in `tests/post-settings-pillar.php`, `tests/post-settings-page-signing.php` and `tests/post-settings-focus-keyword.php` move to the script source and the capping callback. (#1608)

## [17.4.4] - 2026-09-21 — the window takes the station's shapes

### Fixed
- **Three leaves stood a ghost Refresh in for live refresh; the native shape is `os-poll` on a no-op action.** Read against 17.4.3: Integrity > Provenance, Connections > Webhooks and Connections > Cron each painted a ghost `refresh` button where the classic page auto-refreshes, while the three classic pollers (`sn-cron-dashboard`, `sn-provenance-admin`, `sn-admin-heartbeat`) still rode the Dashboard window's script list against markup the kit never paints (`grep` over `apps/` for `.sn-prov-live`, `table[data-webhook-id]`, `.sn-cron-run-now` hits one comment), so each re-armed on every paint, matched nothing, and the Heartbeat client rode every tick with an empty want list. The runtime's own mechanism is `os-poll="30000"` on an element carrying an `os-action` (App Framework, Experimental at OpenStation 1.1.10: `readPolls()` drops an element under 250 ms, so `snt_kit_poll()` floors there, and keys a timer by action plus interval plus args, `reconcilePolls()` skips a tick while the window is minimized, the tab hidden or a dispatch in flight). Not on the declared `refresh`, which drops the notice and the flash on every dispatch and would hide a new webhook secret before it was copied: the app declares `poll`, which re-reads the badge and touches no state, and `snt_kit_poll()` paints the hidden trigger. Two ceilings read in the runtime's morph decide where it rides. The morph keeps only the focused control's value and syncs a kit checkbox's `checked` attribute focused or not, and `os-disclosure` keeps its open state as an attribute the morph strips on every repaint. So Provenance, whose forms are bare submit buttons, polls freely; Webhooks (text fields) and Cron (two settings checkboxes) poll only while their forms are folded behind `sn_watch=1`, a `sn_*` param the way every mode on the classic page is a query param, through one `snt_kit_watch_bar()` (Watch live / Stop watching, both a `go` back onto the leaf), and the watched delivery log paints as an open section rather than a disclosure that would fold itself every 30 s. The provenance trap: `provenance_data()` spent the per-user sweep-result transient on read while the `sn_prov_swept` flag rode `params`, so the second paint of a successful sweep (the ghost's second click today, a tick within 30 s tomorrow) said "Sweep failed"; the first read now moves the result into `params` beside the flag and later paints read it there, so the two live and die together. The backfill result is the same one-shot transient with no flag at all (its redirect carries none), and the same read spent it, so a tick anywhere in the 30 s after a backfill dropped the "Imported N, repaired M" notice and, with no candidates left, the whole Ledger backfill section; it now takes the same stash into `params`, and `go` and `post` reset `params` so it leaves with the next user action. The three poller handles and the two registrars that served only them leave the window (the provenance registrar stays for its sheet); the scripts stay for the classic pages. Pinned: six actions with `poll` leaving every state key untouched while `refresh` still clears (`tests/openstation-app-dashboard.php`), the nine-script window list without the three handles (`tests/openstation-host.php`), each leaf's default paint without a ghost and its watched paint with one hidden poll trigger and no form, the log as a section, and the provenance leaf painted twice through one state saying Sweep complete both times and painting the backfill notice and section both times (`tests/openstation-host-dashboard.php` and the three leaf suites); 25 pins red against the unfixed files. #1607.
- **Suggest and Apply feedback inside a kit leaf paints on the shell's tokens, not light-admin hex on the dark surface.** Read against 17.4.3: `window.sntSetStatus` (`assets/snt-status.js`) wrote `#0a5a1a`, `#6e4d00`, `#8b1a1a` or `#646970` straight onto `node.style.color` for its five callers, the verdict headline (`.snt-verdict-headline--ok / --warn / --err`) and the after-Apply cell states (`.snt-cell-applied`, `.snt-cell-error`, `.snt-suggest-inline-err`) had only the classic sheet's literals, and `freshness-dot.js` set `tone="ok|warn"` on the Caches badge, names `os-badge` has no rule for. On the station's `#1a1721` those hex read at 1.9:1 to 3.2:1: a Saved, Failed or Applying line and every verdict headline were dark text on dark, and the Caches card had no dot. Now `snt-status.js` carries two maps and picks by surface, `node.closest( '.snt-app' )` (the test `health-suggest-actions.js` already uses for os-button and os-modal): the four `--os-ui-*` tokens inside a kit app, the hex elsewhere, because the chromeless iframe paints white and the block editor sidebars carry no admin.css override, so a bare token swap would have put mint on white there. `assets/os-app.css` paints the verdict and cell family on `--os-ui-success-fg`, `--os-ui-warning-fg` and `--os-ui-danger` (13.1:1, 15.5:1 and 5.8:1 on the surface); `freshness-dot.js` sets `success` / `warning`, the names `snt_kit_tone()` already emits for the same card server-side. `os-badge` (Stable) is the fuller port for the headline and `os-save-status` (Stable) for the status pill; the token cut alone makes every line readable and reaches every caller. Pinned in `tests/os-feedback-ink.php`: no hex write on `node.style.color` and the surface test in the script, each of the six selectors on its token in the sheet with comments stripped first, and the two tone names; eleven pins red against the unfixed files. #1616.
- **Health check cards, family subheads, the passing chips, the cron Refresh row, the resume arrows and the static fields are kit shapes, not raw wrappers with no window rule.** Read against 17.4.3: five leaves painted `div`, `h3`, `h4` and `p` wrappers (`snt-check__h`, `snt-subhead`, `snt-chips`, `snt-toolbar`, `snt-rsm-controls`) whose class carried no rule in either window sheet, so they took wp-admin common.css metrics inside the dark leaf; `.snt-toolbar` reached the cron leaf only as a leak from the Signal & Noise window's sheet; the webhooks and login leaves hand-rolled a `.snt-field-static` div for their locked values, a class `os-app.css` does style, where the music leaf already had the kit's field row; and `snt_kit_field( 'checkbox' )` silently dropped a `hint`, which is why two cron toggles carried theirs as a sibling paragraph. Now each Health check is an `<os-card class="snt-check">` (Stable) whose `<header><h3>` row carries the title, the badge and Suggest all, the shape the Links and Cloudflare cards paint and the flex row the card's header rhythm gives; `os-app.css` sizes every `os-card > header > h3` in the app, which common.css gave 1.3em and a 1em margin, so the Tools › Links card titles and the Cloudflare leaf's Purge all caches title take the same 14px and no margin, the two cards whose flex header row that margin was already inflating. The class stays because `health-suggest-actions.js` walks `closest( '.snt-check' )` for Suggest all. Each family is its own `<os-section>` (Stable) and Advisories one with its hint as the description: the dashboard sheet boxes every section body, so a family section inside a Findings section would be a box in a box, and the Findings wrapper goes. A passing family is an `<os-row>` of the family label and an `<os-cluster>` (Stable) of chips, the shape the finding rows use. The cron Refresh row and the resume arrows (in the card's footer slot) are `<os-cluster>`. The webhooks and login static fields go through `snt_kit_static()`, the music leaf's locked-field shape lifted into the kit: `<os-field-row label>` (Stable) around the value and the hint as its sibling paragraph so it may carry inline code. `snt_kit_field( 'checkbox' )` wraps the control in a label-less `<os-field-row hint>` when given a hint, so the cron toggles have the layout every other field has. Identity & SEO keeps `.snt-field-static` as a labelled group around its URL inputs, a fieldset shape the kit has none of. Pinned: `snt_leaf_classic_markers()` now refuses the five wrapper classes across every leaf suite, the health pin moved from the h3 to the card header with a section per family and a chip row, the cluster in the cron and resume suites, the field rows and no static div in the webhooks and login suites, and the kit helper's `hint=`; seventeen pins red against the unfixed files. #1600.
- **The Webhooks and Tags native forms end in the kit's primary button and tick accent checkboxes, not browser defaults.** Read against 17.4.3: both leaves close a native `<form os-action="post">` (the only shape that carries a repeated-name checkbox group, since `<os-form>` collects by name, later-wins, and reads every checkbox as a boolean) with `<button class="snt-submit">` and paint `<input type="checkbox">` rows, and neither `assets/os-app.css` nor `sn-dashboard.css` had a rule for either, so each form ended in the browser's light-grey user-agent button on the dark station surface. Both leaves' docblocks recorded the gap as one port PR's file scope, not a decision. `os-button` and `os-checkbox` (both Stable) are not form-associated (nothing in the kit declares `formAssociated`), so the fix is the look: `.snt-app .snt-submit` borrows os-button's primary chain verbatim (`--os-ui-button-bg`, `--os-ui-button-fg` through `--os-ui-accent-ink`, `--os-ui-button-border-radius`) and the boxes keep wp-admin's `forms.css` as their painter: the shell is a real wp-admin document, so `forms.css` reaches every light-DOM control, sets `appearance: none` and draws the box, the tick and the dot itself, which is why a checked box was already pink through `--wp-admin-theme-color` and why `accent-color` cannot fire on it; the visible gap was the unchecked box, white with a `#1e1e1e` border on the dark surface. `.snt-app input[type="checkbox"], .snt-app input[type="radio"]` paints that box on `--os-ui-field-bg` and `--os-ui-field-border`, the pair OpenStation's `variables.css` names for a plain `<input>` in a window body, and `:checked` on `--os-ui-accent`, the flat accent the kit's `holoCheck` ticks in; one selector for both so the Tags row's radio and checkbox share the 16px and the zero margin (forms.css gave the radio `-0.25rem 0.25rem 0 0`, 4px above the box beside it). The two "styling gap" docblocks are gone and `docs/openstation-compat.md` now names the native form as the second form shape instead of calling the kit form the only one. Pinned in `tests/os-native-form-controls.php` (the `os-grid-auto-fit` shape: comments stripped, then declarations): the submit's fill, ink and radius, the unchecked box's field tokens, size and zero margin with no `accent-color`, the checked box's accent, and both leaves still ending in that button with no gap recorded; eight pins red against the unfixed files. #1595.
- **S&N Home's Continue working empty state is the kit's `<os-empty-state>`, as Station Home paints it.** Read against 17.4.3: with no recent post, `home_continue_working_html()` painted the Needs-attention all-clear div with a `welcome-write-blog` glyph, "Your desk is clear" and "Start something new and it will be waiting here when you return.", a left-aligned two-column row; Station Home paints that exact copy and icon as `<os-empty-state>` (Stable) for this section and keeps the all-clear div for Needs attention only. A port artifact: the copy was adopted through the Needs-attention markup after Station Home had already shipped the component. Now the section calls `snt_kit_empty()`, which this leaf already calls twice, and `sn-dashboard.css` carries Station Home's two-line twin (`min-block-size: 176px` and the bottom border) on `.snt-home__work os-empty-state`; Needs attention keeps its row. Pinned in `tests/dashboard-home-pulse-and-work.php`: with the stub returning zero posts the output carries `<os-empty-state heading="Your desk is clear" icon="welcome-write-blog">` with the description and no `snt-home__all-clear`, and the sheet carries the twin; three pins red against the unfixed files. #1593.
- **Every classic notice is `wp_admin_notice()`, core's shape since 6.4, not a hand-echoed literal.** At 17.4.3, `grep -rn 'notice notice-' inc signal-and-noise-tools.php` found 37 `<div class="notice notice-...">` literals in 22 files, one `printf` with the type as `%s`, and neither `wp_admin_notice()` nor `wp_get_admin_notice()` anywhere in inc; the plugin floor is 7.0, three majors past the function. Read against the 7.1 checkout (wp-includes/functions.php:9196 and :9307): the getter builds `notice`, `notice-<type>`, `is-dismissible`, then `additional_classes` in order, one `<p>` wrap unless `paragraph_wrap` is false, attributes through `esc_attr`, and passes `wp_admin_notice_markup`; the echo fires `wp_admin_notice` then `wp_kses_post`s the whole. Every body the plugin ships is `<p>`, `<strong>`, `<code>`, `<a>`, `<span>`, `<br>`, `<em>` and one `<button>` with `class` and `data-*`, all in 7.1's post context, so nothing is stripped. OpenStation 1.1.x and the theme hook neither the action nor the filter, so the window paints the same bytes. All 37 now go through the function: `type` is the one word, `notice-alt` and `inline` ride `additional_classes` (wp-admin/js/common.js moves every `div.notice` that is not `.inline` under the page heading, and the tab readouts stay where the tab put them), the three multi-paragraph readouts pass `paragraph_wrap => false`, the two return-string sites in inc/machine-readers-render.php use the getter and keep escaping at the caller, the prepop notice's `data-post` rides `attributes` with `.sn-prepop-notice` in `additional_classes` (class order moves behind `notice-info`; assets/prepop-notice.js reads the selector, not the order). The flash renderers drop their own `wp_kses_post` on the body, since the echo runs it on the whole. Two notices sit inside a `<form>` (the analytics token readouts in inc/analytics-render-settings.php and the prepop notice in the post meta box) and were ported like the rest: the markup is byte-identical for the analytics readouts, the prepop div's class order moves as above, and no form control is in a body. The one `<p class="sn-mr-truncated notice notice-warning inline">` stays a paragraph, since the getter only emits a div. What the port buys is one seam: a future pin, audit-log entry or shell filter sees every classic notice from one hook instead of 22 files. Pinned in tests/classic-notices-native.php: a walk over inc/ and the bootstrap finds zero `<div ... notice notice-` literals on code lines (37 against 17.4.3), at least 35 calls (the vacuity guard, 0 against 17.4.3), and every quoted word from a call's `'type' =>` to the end of its line one word with no space (core `_doing_it_wrong`s a `type` with a space, functions.php:9196), and one representative per file group renders the classic bytes and is seen by the action or the filter; 11 red against the unfixed code. The stub, tests/lib/wp-admin-notice-stub.php, is core 7.1's getter with two named departures (array_merge for `wp_parse_args`, a throw where core `_doing_it_wrong`s the spaced `type`, so a fixture cannot paint what core refuses) and its echo is core's own `wp_kses_post()`: it loads wp-includes/kses.php and the HTML API from the `SNT_WP_HTML_API` seam the host suites use (ci.yml now fetches kses.php beside the seven HTML API files), so the six representative byte pins are measured through the runtime's filter, and a negative control proves that filter strips `<script>`, `onclick`, `<input>` and `<form>`; without a checkout the two kses pins SKIP locally and FAIL under CI, as tests/openstation-host.php does. The stub feeds the leaf harness and the twelve standalone suites that render a notice; a suite that stubs its own `wp_kses_post()` first keeps it. #1618.
- **A native window's ability calls rode wp.apiFetch, so the title bar's status ring never moved; the shell's fetch is wp.os.fetch.** Every station-side ability call goes through `window.sntAbilityRun`, and the runner sent it with `wp.apiFetch` (`assets/snt-ability-run.js`): inside S&N Home, Suggest, Apply, uptime detail, cron run and unschedule were invisible to the shell, the `os-save-status` ring neither breathed nor filled nor banged, and the shell's 401/403 fast path never saw the response. `assets/provenance-admin.js` was the other transport, a bare `fetch()` with an `X-WP-Nonce` read from the `data-nonce` the server painted once at render; nothing rewrote it, so a window open past the nonce window polled with a stale one and the commits table read "Status check failed (HTTP 403)" every 30 s, the trap 17.3.0 closed for four other scripts. Read: `wp.os.fetch( input, init, opts )` is Stable, "every HTTP call from a plugin" (`docs/javascript-reference.md`, `src/boot/tracked-fetch.ts`, 1.1.10): it drives the target window's activity phase (the focused window by default, which a click inside S&N Home already is), stamps `openStationConfig.restNonce` on same-origin REST URLs when the caller set no header, and `opts.silent` skips the phase; it resolves with a native `Response`, where `wp.apiFetch` resolves with parsed JSON and rejects with the parsed `WP_Error` body, and callers read `err.data.retry_after`, `err.message` and `err.code`. Changed: the runner sends through `wp.os.fetch` when `typeof window.wp.os.fetch === 'function'` and keeps `wp.apiFetch` as the classic-page and editor-iframe path, mapping `res.ok` to `res.json()` and a refused response to the parsed body as the rejection, so no caller changed, and a body that is not JSON (an HTML 503 from Varnish, a challenge page, the WAF's 403 page) to the same `invalid_json` rejection apiFetch's `parseJsonAndNormalizeError` makes, carrying the HTTP status the raw `SyntaxError` lost; the URL is built on `rest_url()`, localized as `root` beside the verb map in `inc/ability-run-client.php`, joined with `&` when the root already carries `?` (plain permalinks); `options.silent` is forwarded, and the six desktop widgets that refresh on a timer or at mount pass it (a widget is not a window, so its poll would breathe the ring of whatever window the owner last clicked); the provenance poll rides the shell's fetch, silent, with no hand-set header, and keeps the DOM-nonce bare fetch for the classic tab. `opts.source` is declared but unread at 1.1.10, an upstream gap, not waited on. Pinned: the runner contains both transports behind the typeof guard, forwards `silent`, keeps the apiFetch contract, rejects a non-JSON body as `invalid_json` with the catch on `res.json()` itself (before the ok test, so a JSON `WP_Error` is never masked) and builds on the localized root; the localizer hands `root` as `rest_url()`; each timer widget's background call pinned by slug with `silent: true` (a file-wide match could not tell the anchors widget's status load from its click-driven sweep, which stays loud); the provenance poll's shell branch and the one `X-WP-Nonce` literal on the fallback line itself (count 1 alone passed a shell branch that sent the stale nonce from a variable); `/wp-abilities/` stays confined to the runner (Group D). Seventeen pins red against the unfixed files. #1601.
- **Reschedule and the Apply writes told no other window; the native shape is wp.os.announceContentChange.** `assets/os-posts-reschedule.js` POSTed to the plugin's own reschedule route and returned, so the shell refreshed only the window that ran it; every Apply in `assets/health-suggest-actions.js` went through `callAbility` and repainted only its cell. `grep announceContentChange` over 17.4.3 returned nothing. Read: the five post writes run `wp_update_post`, which OpenStation's `wp_after_insert_post` publisher records and the Heartbeat catch-all relays 15 to 60 s later, one tick late; the alt-text write is `update_post_meta` and the orphan delete is `wp_delete_attachment()`, which fires `delete_attachment`, `delete_post` and `deleted_post`, never the `before_delete_post` the Recycle Bin records on, and `includes/content-changes.php` registers nothing on post meta or attachment deletes, so the Media Library window never learned of either, not even on a heartbeat. `wp.os.announceContentChange( type, action, ids, source )` is Stable (`docs/javascript-reference.md`, `src/broadcast.ts`): "whenever your window mutates content through your own REST endpoints"; the server half, `openstation_content_changes_record()`, is Stable (`docs/hooks-reference.md`), the documented call for a plugin's own mutation paths. Changed: the reschedule announces `post`/`updated` for the ids on the ok branch, plain (the script runs only in the shell); `callAbility` announces on resolve when the slug is in the apply column of `ABILITY_BY_CHECK`, `attachment`/`deleted` for `ai-orphan-apply`, `attachment`/`updated` for alt text, `post`/`updated` for the rest, behind a `typeof` guard because the same script runs on the classic leaves; source tag `signal-noise`, not the app runtime's per-window tag, so the S&N app window still refreshes on an Apply from S&N Home; `inc/ai-alt-text-suggest.php` and `inc/ai-orphan-suggest.php` call the recorder after their writes behind `function_exists`, so the MCP door and a classic leaf in a chromeless frame reach the heartbeat path too. Pinned: the reschedule announce sits between the toast and the `undefined`, once, after the ok test; the Apply seam's guard, slug set, both producer calls and its place on `callAbility`'s resolve; the recorder called exactly once with `attachment, 200, deleted` after a landed orphan delete and never after a refused one, and with `attachment, 5, updated` after an accepted alt text and never after an empty one. Ten pins red against the unfixed files. #1621.
- **The S&N admin bar printed raw script and style tags on every surface, and on the shell document its quick actions died with the page-load nonce; the native shape is core's own `admin-bar` handles and, on the shell, the ability twins over the plugin's one transport.** Read against `inc/admin-bar.php` at 17.4.3: `sn_admin_bar_print_script()` echoed a `<script>` block on `admin_print_footer_scripts` and `wp_print_footer_scripts`, `sn_admin_bar_print_style()` a `<style>` on the two print_styles hooks, and every node minted one `wp_create_nonce()` at page load. The shell (`admin.php?page=openstation`) is an ordinary admin page where core builds the bar and OpenStation hides it with CSS unless the per-user mode says static or dynamic; it has no bounded lifetime, so after `wp_nonce_tick` (a day) `check_ajax_referer` answered 403 with a bare `-1`, `r.json()` parsed it as a number, and the owner saw a red toast reading "Done.". Core enqueues `admin-bar` in `WP_Admin_Bar::initialize()` on admin and front end alike with `admin-bar.js` in the footer, and OpenStation's own toggle attaches its CSS the same way (`includes/admin-bar.php`). Now both blocks ride `wp_add_inline_script( 'admin-bar', ... )` and `wp_add_inline_style( 'admin-bar', ... )` on `admin_enqueue_scripts` and `wp_enqueue_scripts`, where `get_current_screen()` and `is_singular()` are both resolvable for the two guards; the JSON config and the nonces stay per pageload and the block prints once, after `admin-bar.js`, exactly where it printed. On the shell request (`openstation_is_shell_request()`, Stable, guarded with `function_exists` because the front end has no OpenStation) each item declares its twin in `sn_admin_bar_items()` and the node carries `ability` + `input` instead of `nonce`: Force Update Check is `get-deploy-status` `{ force_refresh: true }`, Scan Pattern Adoption `pattern-adoption-scan`, Purge All Caches `purge-all-caches`, Clear DB Overrides `clear-template-overrides`, Regen OG Card `regenerate-og-card` (moot there, the contextual guard resolves no post), and Purge Cloudflare `purge-all-caches`, since no ability purges the zone alone and that one's message names the Cloudflare verdict, which is what the dashboard button does. The shared runner `snt-ability-run` is enqueued on that request and the click goes through `window.sntAbilityRun` over `wp.apiFetch`, whose nonce middleware refetches on `rest_cookie_invalid_nonce`; the destructive gate is `wp.os.confirm( { title, message, danger } )` (Stable; traps focus and restores it to the opener, the property the `window.confirm` comment wanted) and the result lands as `wp.os.showToast( { message } )` (Stable) at the top of the shell instead of a green box painted at `top:46px; right:20px` over the desktop. The admin-ajax POST is never routed through `wp.os.fetch`, which stamps `X-WP-Nonce` on REST URLs only and would leave the body's `_ajax_nonce` the page-load one. Outside the shell nothing moves: the six admin-ajax handlers, the per-pageload nonces, `window.confirm` and the toast stay for the classic admin and the public front end (the runner is admin-only and three of the six actions have no other one-click surface there). The second review caught what the substring pins could not: the first cut's `toast()` read an `os` declared inside the click listener, a free variable in its own scope, so every click on every surface ended in `ReferenceError: os is not defined` after the request had fired and the label restored, and no toast painted at all, the shell and the classic admin alike, a regression from 17.4.3 which painted it; `toast()` now resolves `wp.os` at each call through one `shellOs()` (never captured at load: `desktop.min.js` is deferred and installs `window.wp.os` after this block runs). The same review read the stacking: core's `#wpadminbar` sits at z-index 99999 and `<os-confirm-dialog>` at 10000 with no rule of ours between them, so on the shell a second click while the dialog was open mounted a second dialog and confirming both fired `clear-template-overrides`, a force-delete with no undo, twice; the busy flag is now taken before the gate and handed back on decline, so the row is re-clickable after a No and inert while the question is open. Pinned in `tests/admin-bar-quick-actions.php`: both attachers echo nothing and record the `admin-bar` handle; on the shell no node carries a nonce, every node an ability, the five slugs and the force_refresh input, the confirm still delivered byte-identical, the busy flag before `os.confirm(` and `os.confirm(` before the label swap, `os.showToast(`, `sntAbilityRun(meta.ability` with no `/wp-abilities/` path built, no `os.fetch`, and the runner enqueued there and not off it; off the shell every node still carries a nonce and the body `_ajax_nonce`. And, because every one of those held while the block threw, the recorded block is now executed: `tests/js/admin-bar-quick-actions.mjs` runs it under node per surface with only DOM, fetch, the runner and `wp.os` faked, and the suite mirrors each scenario (a click paints one toast with the response's message and no unhandled rejection; two clicks on the destructive item open one confirm; declining leaves the row untouched and re-clickable; confirming dispatches once), a missing scenario red rather than silent. Thirty-two pins red against 17.4.3, nine against the first cut, 116 green with the fix; `contracts-stub`, `desktop-mode-integration`, `og-card-password-protected` and `desktop-mode-boot-order` unchanged. #1623.
- **Three Site Health tests and three Info-panel facts reach a leaf; the native shape is the 17.4.0 kit box.** Read at 17.4.3: `sn_family_drift` (inc/family-drift.php), `snt_ml_reader_anomalies` (inc/ml-reader-anomalies-health.php) and `sn_ssrf_pinning` (inc/ssrf-guard.php) register as core Site Health direct tests and no leaf, classic or native, painted any of them; outside their own files only the abilities read the first two and nothing read the third. Two Info panels sat the same way: which plugin serves the `wp-*` JS handles and the database template/navigation override count (inc/dash-debug-info.php), and the plugin-directory size with the last janitor sweep (inc/plugin-footprint.php), copy-to-clipboard-only facts on a classic screen the window does not reach. Now each verdict is a box where its question is asked, painted from exactly the reads the Site Health row and the abilities make, nothing computed on the leaf: Measurement > Machine Readers carries "Crawler-family drift" and "Reader behaviour" as one `.snt-cols` row of two boxes of a height under the sensor hero, and Security > Firewall carries "Outbound pinning" at full width under the edge posture with the two facts the verdict derives from (the cURL transport, the last unpinned request from `sn_ssrf_unpinned_last`); the in-code reason that row stays off the curated Health tab (it never fires on a healthy host) argues against the defects tab, not against a posture box, which is the Breached-passwords shape. The verdict paints through one new kit helper, `snt_kit_verdict()`: the summary as the hint line when the status is good, else as a notice, danger for critical (a mirror disagreement is "a RED, not a note" in the module's own words) and warning otherwise. The three facts are rows of a "Plugin environment" box under the reports on Integrity > Reports, in every state of the scan; the footprint's per-entry rows are private on the Info panel (filenames), so the leaf paints the total, the legacy-leftover count and the sweep line only, and the sweep sentence moved into `sn_footprint_janitor_line()` so the panel and the leaf paint one derivation of one option. Stable kit components on the App Framework surface the dashboard already runs on; no shell seam. Pinned on the three leaf suites: each heading in the paint, the row placement (between hero and columns; under the posture with the one `.snt-cols` row untouched; under the last report row), a never-run and a sensor-unread verdict as warning notices, mirror parity failed as a danger notice naming the plugin-only family, the good verdicts as hint lines with no notice, a deviation and a silence with both counts, an unpinned request as a warning naming the host with the row toned and dated, the pinning box before the monitor ever ran, the JS-package row naming `plugin:gutenberg`, the override count, a formatted total, the sweep sentence from the same option, one legacy leftover counted over a fixture directory, and no sweep row when no sweep is stored; twenty-one pins red against the unfixed leaves (eight, six and seven). #1599.
- **A dead sensor cost the Machine Readers leaf two 6 s waits per paint, not one; the native shape is one outbound read per window per request.** Read on the #1599 branch with the real anomalies pipeline loaded and a transient store that keeps successes like production: sensor down, one `snt_leaf_paint('monitoring','machine-readers')` made two calls to the 30-day aggregate endpoint (main made one), because `snt_mr_fetch()` caches a success only, on purpose (a dead sensor must not be remembered for 15 minutes), so the hero and `snt_ml_reader_anomalies()` each went to the network and each waited the 6 s `wp_remote_get` timeout; the OpenStation app runtime has no dispatch timeout of its own (openstation `src/app-runtime/session.ts` aborts on disposal only), so the window spun for 12 s. The abilities read the same window, so the root is the shared function, not the leaf: `snt_mr_fetch()` now memoizes every outcome per `snt_mr_cache_key()` for the rest of the request, in a static the transient never sees (`snt_mr_memo()`), and `snt_mr_cache_flush()` (the settings save, a key rotation) empties it with the transients. The leaf is unchanged but for its comment. Pinned on tests/machine-readers-api.php: two 30-day callers on a failed read make one `wp_remote_get`, the second gets the failure verbatim, another view is another call, the failure stands within the request once the sensor answers, a flush lets the next read out, and a success is memoized with no transient store; five pins red against the unfixed function. The API, snapshot and leaf suites mark a new request with `snt_mr_memo( null )` where they change the sensor's answer. #1599.
- **Four plumbing gaps, each closed with the native shape.** (1) The five `signal-noise/v1/analytics/*` REST routes registered no `args`, so `?range=60` fell through `snt_analytics_resolve_range()`'s 7-day fallback and answered a week labelled as nothing, the trap #1239 had already closed on the ability path. Read core 7.1: `register_rest_route()` merges a route's `args` into every method group, an arg with a `type` and no sanitizer gets `rest_parse_request_arg`, and an enum miss is `rest_invalid_param` 400 with the parameter named before the callback runs. One `$window_args` array now rides all five routes: `range` a string enum derived from `SN_ANALYTICS_RANGES` plus `all` (default `30`, the header's claim made true), `class` an enum of `SN_ANALYTICS_CLASSES` (default `human`), and `property` on event-props a plain string, not `sanitize_key`, because ingest stores property names verbatim. `OPTIONS` on any route lists the vocabulary. The suite's resolver stub fell back to 30 where production falls back to 7, a fixture looser than the runtime; it now requires the real `inc/analytics-admin.php`. Pinned in tests/analytics-rest.php (11 red against 17.4.3) and tests/analytics-rest-anomalies.php (1 red; its fixture fired the closure without the two constants production loads first, and now requires the real ranges file). (2) The plugin header carried no `Update URI`, so every update check posted the slug to api.wordpress.org unmarked, the accidental-collision case the 5.8 header exists for. `Update URI: https://github.com/juanlentino/signal-and-noise-tools` now names the repo `inc/wp-update-integration.php` reads tags from; with no `update_plugins_github.com` hook core's loop returns `false` and the existing `pre_set_site_transient_update_plugins` filter runs as before. Pinned in tests/manifest-floor.php (1 red). Porting the transient filter to the `update_plugins_github.com` hook is the issue's second, independent step and is not in this PR. (3) `docs/openstation-compat.md` said the `open_url` remap gap was "filed and fixed alongside" WordPress/openstation#819; read 2026-09-20, #819 is OPEN with `mergedAt: null` and no tag contains its commit. The sentence now states the state and records the two interceptors that exist because of it (`armDoorClicks`, `openLink` asking `tryNativeRemap` first, 14.7.6). Pinned in tests/openstation-compat.php against the doc AND the code, so it fails in either direction (3 red). (4) `inc/cron-dashboard-admin.php` shipped 25 strings as a `sntCronI18n` localize object beside a `wp_set_script_translations()` call the script never read: two string sources for one script, and the 25-entry `var sntCronI18n` inline object rode every S&N admin page and the Dashboard host. Read core 7.1 `WP_Scripts::print_translations()`: with no JSON translation file (the plugin ships no `languages/` dir) `load_script_textdomain()` returns false and no per-handle `setLocaleData` block prints, before or after this change; the one `setLocaleData` on the page is wp-i18n's own text-direction inline, and it printed on main too because `set_translations()` had already appended `wp-i18n` to the deps. The documented way: `wp-i18n` in the deps, the localize block gone, and `assets/cron-dashboard.js` reads `wp.i18n.__()` under the exact domain `signal-and-noise-tools` (eight sibling scripts pass `signal-noise-tools` and fall back to English in silence; that is its own issue) with `wp.i18n.sprintf()` for the templates. The Run now, Running, fired-in and history labels read as before; `window.sntCronI18n` is gone. Pinned in tests/openstation-host.php (deps, translations set, no localize object) and tests/openstation-host-assets.php (source reads the registry, no hand fills, no per-call domain) (6 red). #1620, #1615, #1619, #1611.
- **The fourteen remote-door twins rode every Ask AI turn and sat in the agents Tools picker, and neither surface can run one; the native shape is the two OpenStation seams that shape those lists.** Read in OpenStation 1.1.10: `includes/ai-copilot/abilities.php` enrols every ability carrying `annotations.readonly` as a Copilot tool and `includes/ai-copilot/search.php` serialises its name, description and input schema into every turn; `includes/agents/abilities.php` projects every registered ability into the picker; both run the pick in-process, never through the bridge, and each twin's permission callback passes only a caller holding `sn_read_remote_analytics`, which only the bridge's own `execute()` grants. So every call either surface could make is refused inside `WP_Ability::execute()`. `snt_dm_ai_pruned_abilities()` now appends `sn_mcp_remote_slugs()`, the list the remote guard already owns, and the existing `openstation_ai_tools` prune (Stable) drops the fourteen from the Copilot list; `snt_dm_agent_catalogue_prune()` drops the same slugs on `openstation_agent_abilities_catalogue` (Experimental, no `desktop_mode_` alias), hooked on `plugins_loaded` at priority 10 behind `function_exists( 'openstation_agents_abilities_catalogue' )`, because the applying file loads at priority 5 and only while the agents feature is on, so a load-time guard would never pass. The abilities stay registered and reachable through the bridge. Pinned in `tests/desktop-mode-integration.php`: a `remote_*` name in the six-tool fixture is gone after the prune, every guard slug is on the prune list, the picker seam is unhooked before the picker function exists and hooked after, a remote row is gone from the catalogue while a local row and an OpenStation row survive; six red against the unfixed code. #1594.
- **Every agent-run turn rewrote the Anthropic request body through `http_request_args` after the AI client had built it; the native shape is `openstation_ai_model_config`.** Read in OpenStation 1.1.10: `docs/hooks-reference.md` and `docs/examples/ai-model-config.md` ("Spend effort only where it pays"), `includes/ai-copilot/client.php` (`max_tokens` through `ModelConfig::setMaxTokens`, `custom_options` through `setCustomOptions`, a string `model` through `using_model_preference`), `includes/agents/runner.php` passing `source => agents/runner`, and the SDK's `PromptBuilder`, where a model preference is soft: picked when the connector lists it, otherwise the client's own choice stands. Both removal conditions the module's docblock named are met (openstation#530 merged, #531 closed by #549 in 1.1.0). `inc/openstation-agent-output-budget.php` now returns `max_tokens` 8192, `custom_options` `thinking: {type: adaptive}` and `output_config: {effort: low}`, and `model` `SN_AI_DEFAULT_MODEL` when the source is the agents runner; any other source comes back untouched, and an effort outside low, medium, high returns the config unshaped, so the ceiling is raised only when thinking is bounded. Gone: the `PHP_INT_MAX` `http_request_args` shaper that stayed hooked for the rest of the PHP request, the JSON decode and re-encode of every outbound body, the runner pre-filter arm, and the Claude 5 gate read off the body, which became the model pin since the seam carries no resolved model. `snt_agent_anthropic_effort` and `snt_agent_anthropic_max_tokens` keep their names. The callback is deferential, as the module it replaces was (rule 3 of its docblock): a config an earlier callback already gave `thinking` or `output_config` comes back byte-identical, a higher `max_tokens` stays, sibling `custom_options` keys such as `metadata` stay, and a `model` already chosen stays, so the day the runner feeds the stored per-agent model override into the seam the pin cannot turn an Opus choice into Sonnet. Experimental seam, guarded on `openstation_ai_apply_model_config()` at `plugins_loaded`. `docs/openstation-compat.md` moves the seam from "not consumed" to the consumer. Pinned in `tests/openstation-agent-output-budget.php`: the shaper and the arm are undefined, nothing sits on `http_request_args`, the guard fails without the client function and hooks at priority 10 with two accepted args after it, the runner config carries exactly the two provider-native keys and the ceiling, every other source and a foreign config are byte-identical, a priority-5 runner config carrying `thinking`, `metadata`, 16384 and a model comes back byte-identical, a partial one keeps its ceiling, its `metadata` and its model beside the added keys, the model pin reads the constant at filter time and fills only a gap, `http_request_args` stays empty after `plugins_loaded` has fired with the client present (a re-arm inside the closure goes red), the two filters, and no JSON round trip in the module; twenty-three red against the unfixed code. The memory rule `thinking-is-ceiling-bounded-without-effort` still names the old route and is the owner's to update. #1613.
- **Copilot turns are metered on AI > Models & Budget, on a line of their own beside the cap.** Ask AI runs on the same connector and the same key as Suggest and Insights, but the plugin's ledger saw its own `generate_text_result()` calls only: no file under `inc/` subscribed to the station's completion hook, so a Copilot turn cost nothing on the leaf. Read in the OpenStation checkout (1.1.10): `openstation_ai_search_completed` is Stable in `docs/hooks-reference.md`, fired on the three main run paths in `includes/ai-copilot/search.php` with `usage` summed across every turn and `model` from the AI Client, either null when the provider did not report it; the follow-up leg dispatches it with neither. Its old name `desktop_mode_ai_search_completed` fired at v0.9.8 and was renamed by #475 like the rest of the compat table, so `inc/ai-copilot-spend.php` dual-registers through `snt_os_compat_add_action()` with the option increment behind `snt_os_compat_seen_once()`, as `inc/ai-tool-invocation-log.php` does. It prices the turn with the existing `snt_ai_estimate_cost()` (the station's `model.id` is the pricing table's id space) and folds it into `sn_ai_copilot_spend_month`, a `YYYY-MM` option of its own with the rollup's shape, window and zero guard. Not into `snt_ai_add_month_spend()` or the feature ledger: the cap can only refuse the plugin's own features, so counting Ask AI against it would pause Suggest sooner without ever pausing Ask AI, and the feature buckets must keep summing to the total the cap reads. A turn it cannot price (`usage` or `model` null or absent, an id the hand-kept table lacks, zero tokens) is counted in `sn_ai_copilot_unpriced_month`, never dropped: the station picks its model on its own side, so the first unpriced Anthropic id would otherwise turn the row into a silent $0.00 that reads the same as no Ask AI. The leaf paints the figure as one more row inside the "This month, by feature" box (the row pairing of #1573 holds), marked as not counted against the cap, with a hint that it is priced at list rates from the reported tokens with no prompt-cache discount, and, when the count is above zero, the Insights leaf's sentence: N turn(s) reported no usage or a model with no list price on file and are not in the dollar figure. "Spent this month", the meter and the feature rows are exactly as before. `docs/openstation-compat.md` carries the twelfth hook row with both names. Pinned: `tests/ai-copilot-spend.php` fires the hook and reads 0.0105 for a 1000-in 500-out Sonnet 5 turn while both ledger readers stay at zero, counts `usage` null, `model` null, the follow-up leg's bare payload, an unpriced id and a zero-token usage as unpriced turns while a non-array payload moves nothing, binds under both hook names and records the same payload once across them and twice within one family, accumulates a second model at its own rate, prunes both options to the window on synthetic `2020-NN` keys (a seed walked back with `strtotime( "-N months" )` collapsed two steps onto one key on the 29th, 30th and 31st and went red on those dates) and reads a damaged option as zero (the file is absent on the unfixed tree, so the suite fatals there); `tests/os-leaf-ai-models-budget.php` pins the row inside the box, the recorded zero, the list-rates wording, the unpriced hint present at 3 and absent at 0, the feature rows' decimals rule and the untouched total and meter, eight red against the unfixed leaf. #1597.
- **The SN command palette toasts into a no-op, reloads the station to open a tab and confirms in the browser dialog; the native shape is `wp.os.showToast`, `ctx.openInWindow` behind the plugin's own remap, and `wp.os.confirm`.** Read against OpenStation v1.1.10 source and docs: `toast()` in `assets/desktop-mode.js` called `wp.os.notify`, the PWA browser-notification entry whose first guard drops any intent without a `title` (`src/pwa/notify.ts:95`), so all 30 toasts were silent and the 15 commands that end in one showed nothing since upstream #705 made them run at all; `run()` returned nothing, so the palette's own answer surface stayed empty too. `navigate()` assigned `location.href` on the shell document, a top-frame load that `admin_init` redirects back into the shell, rebooting the desktop and restoring the session to open one window. Full reset prompted through `sntConfirm`, which is never enqueued on the shell, so it landed on `window.confirm`, the call the shell's own lint forbids; inside the S&N Home window the Cron leaf's Run now and Unschedule still opened the 230-line `snt-confirm-backdrop` modal beside kit buttons that already use `os-confirm-dialog`. Now `toast()` is `wp.os.showToast( { message } )` (Stable) and returns the message, every `run()` returns its chain so the palette paints the verdict; `navigate( url, ctx, title )` keeps the same-origin check, tries `window.sntOpenStationPreferences.tryNativeRemap( url )` (the plugin's `wp.os.openWindow` opener, preference-gated) and closes the palette when it takes the URL, else `ctx.openInWindow` (CommandContext, Stable), never `location.href`; Full reset asks `wp.os.confirm( { title, message, confirmLabel, danger: true } )` (Stable) with `sntConfirm` as the fallback; and one guard at the top of `sntConfirm()` in `assets/snt-confirm.js` returns `wp.os.confirm()` when the shell has it, so every caller, imperative and declarative, gets the station's dialog, while classic pages and iframe windows (whose `wp.os` carries no `confirm`) keep the modal. Pinned by executing the file against a recording shell in `tests/js/desktop-mode-boot.mjs` (one `showToast` call with `message` and zero `notify`, `run()` returning the message, the remap taking the URL and closing the palette, `openInWindow` when the remap declines with `location.href` untouched, `wp.os.confirm` with `danger: true` and no `window.confirm`) and by source in `tests/desktop-mode-integration.php` (no `location.href`, no `window.confirm(`, `showToast` not `.notify(`, remap before `openInWindow`, the `snt-confirm.js` guard `return window.wp.os.confirm(` before the backdrop, the `return` token itself so a guard that calls without returning, which would open the station's dialog and then the modal beneath it, goes red, and `danger: !! opts.danger` rather than any `danger:`); eleven pins red against the unfixed files, and the two tightened source pins red against a `return`-less guard and a `danger: false` literal. #1606.
- **The Preferences tab's save line is the kit's `<os-save-status mode="pill">`, not a hand-painted paragraph; two shims for shells the plugin no longer runs against are gone.** `assets/os-settings-tab.js` minted a `<p role="status">` with inline margin, font size and a colour token, and the toggle handler wrote the three phases into it by hand: six `textContent` plus `style.color` pairs for Saving, Saved (and the MIO "next reload" variant) and Could not save, repeated after the failure re-render. Read in OpenStation 1.1.10: `src/ui/components/os-save-status/os-save-status.ts` is `help.status: 'stable'`, its `phase` prop (`idle | pending | saving | saved | failed`) is documented "Set manually for one-off integrations", which this is, since the save is the plugin's own REST call and not the shell's `os-settings-save-lifecycle`; `error` is shown in pill mode, `saving-label` and `saved-label` replace the two labels; the window title bar creates one (`src/window/dom.ts:641`), so the tag is defined before any tab renders, the same footing `os-section` and `os-checkbox-label` already stand on. The tab now creates the pill with `mode="pill"` and the translated `saving-label`, sets `phase` on each transition, carries the failure text in `error` at both failed sites (the order against `phase` is immaterial to the paint, the kit's base class collapses same-tick attribute writes into one microtask render, `component.ts` `_scheduleRender`; `error` first only completes the detail of the `os-save-status-change` event), and rides the MIO line on `saved-label`, shortened to "Saved. MIO changes on reload.": the pill's label is capped at 200px with an ellipsis (`os-save-status.styles.ts`, no CSS prop widens it) and the old line measured 209px under the system-ui fallback (clipped to "next rel...") against 199px under Geist, the short one 166px and 157px, read in a headless Chromium render of the 1.1.10 bundle. Three visible changes, every one the kit's default and the same the window title bar wears: the pill fades 2.2 s after `saved` and 6 s after `failed` (`auto-clear-saved-ms` / `auto-clear-failed-ms`; the toggle snaps back at once and the pill announces as `role="alert"`) where the paragraph kept both until the next toggle; a 12px hollow accent ring sits at rest under the five toggles (the indicator is always visible, the styles' "ready LED"; the component's own `idle: hidden` header and "collapses to invisible while idle" help text describe the label, not the ring) where the empty paragraph painted nothing; and a label longer than 200px clips. Two dead shims went with it: the `#snt-preferences-icon` style that masked `.os-settings__nav-glyph-blank` on our tab (the shell paints that spacer only when a registration names no icon, and this one passes `icon: 'bell'`, read from 1.1.9), and the `document.querySelector` plus `removeChild` beside each `Dock.removeSystemItem` in `syncDockTiles` that looked for `data-tile-id` / `data-id` (no rail emits either; the rails carry `data-icon-id` and `data-system-id`, and the `wp.os.refreshMenu()` after every save rebuilds both rails from the payload, which drops a hidden placement). Pinned in `tests/openstation-preferences.php`: the pill's tag and mode with no `role="status"` paragraph, no `style.color` or hand-written phase text, the three phases with `failed` set twice (in place and after the re-render), `error` carrying the failure text at both failed sites (two matches, not one), the MIO line on `saved-label` and the short line in place of the long one, no glyph mask, no scrape; eight pins red against the unfixed file. Driven once through a vm with a fake `os-save-status`: the saved path logs `saving` then `saved-label` then `saved`, the failed path logs `error` then `failed` on both the live and the re-rendered pill, and nothing touches `document.querySelector`, `getElementById` or `head`. #1610.
- **Four setup sequences in kit leaves were browser ordered lists; the kit paints them as os-steps.** Read 2026-09-20: the three owner steps in AI › MCP Clients, Connect a client, the four local-config steps inside its Claude desktop app fold, the four Cloudflare Worker setup steps in Monitoring › Analytics (the second carrying the `wrangler` os-code block) and the four credential steps in Monitoring › Search Console, not configured, were each an `<ol class="snt-plain">` of `<li>`s built by string concatenation, so the numbers were the browser's default markers in the browser's default list typography, while `.snt-plain` is one rule, margin and padding. OpenStation's `<os-steps>` with one `<os-step>` per step is the native shape, Stable on both (`src/ui/components/os-steps/os-steps.ts`), its help block naming this exact use (onboarding, setup flows) and its numbers a CSS counter in the kit's chips; the step body is the default slot, so the os-code block slots straight in, and a bare `<os-step>` with no title is the single-sentence step every one of these is. The app runtime already upgrades undefined `os-*` tags in a painted leaf, so nothing is enqueued. `snt_kit_steps()` joins `snt_kit_code()` in `inc/openstation-kit-display.php`, one `<os-step>` per already-escaped body, and the four call sites hand it their strings; the sentences, the doors, the code blocks and the folds are byte-identical, and `.snt-plain` stays for the eleven bullet lists that read it. Pinned in the three leaf suites: two `<os-steps>` and seven `<os-step>` on MCP Clients with the Application Password door inside the first step and the four fold steps under the "Show the 4 setup steps" hint, one `<os-steps>` of four on Analytics with the wrangler block inside the second step (the suite's unconfigured fixture already drives `sn_analytics_pipeline_complete()` false), one of four on Search Console with the ENABLE sentence in the first, and no `<ol` on any of the three; nine pins red against the unfixed leaves. #1602.
- **The S&N app's icons-or-list choice is a per-user preference in the framework's store, not a localStorage row in one browser.** The native shape is `$os->stored()` / `$os->store()` (App Framework, `docs/app-framework.md`, the `$os` table and the porting table's "User meta / options for the window's own preferences" row), which the WordPress adapter keeps in one user-meta row so the value follows the user across devices; the WP Explorer's own switch is wired exactly this way (`apps/my-wordpress/parts/actions.php`, `my-wordpress.os.ts`). Read: the client kept `sn-signal-noise-view` in `window.localStorage`, read through at paint time, so the phone PWA and the desk disagreed, a second browser started on icons and cleared site data reset it; its comment claimed "there is no post-hydration hook", but the server `mount` closure's state IS the hydrated state (`class-runtime.php` answers `'state' => $state->all()`, `session.ts` `apply()` adopts it), and v13.109.15's seed had run in the client `mounted()` hook, a different moment. Changed: `mount` seeds `state.view` from `$os->stored( 'view', 'icons' )` clamped to `icons|list`; a `view` server action clamps the bound value and `$os->store()`s it; the toggle's `os-pick` runs the local `set-view` flip for an instant switch and then `void ctx.dispatch( 'view' )`; the toggle and the body paint from `state.view`; `VIEW_KEY`, `readView`, `currentView`, `writeView` and their comment block are gone, the stale schema comment is fixed, and the seam is recorded in `docs/openstation-compat.md` (Experimental at 1.1.10). No migration of the old key: a viewer who had picked List starts on icons once and picks it again, after which it follows them. Pinned: the `Os` stub gains `stored()` / `store()`; the seed, the unknown-value clamp, the action's store and its clamp in `tests/openstation-app.php`; no `localStorage`, the pick-then-dispatch shape, the toggle and body on `state.view` and the schema comment in `tests/openstation-app-client.php`; ten pins red against the unfixed code (4 + 6). #1605.

