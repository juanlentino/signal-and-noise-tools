.DS_Store
*.log
vendor/
# composer.lock gitignored: distributed plugin, deps are dev-only linters
# (PHPCS/WPCS) that never ship — repo-wide pinning adds churn for no benefit.
composer.lock
node_modules/
.idea/
.vscode/
# Claude Code session state + nested git worktrees (worktree-local, never committed).
.claude/

# Internal AI-assisted-development scaffolding (specs, plans, handoffs) — local only
docs/superpowers/

# AI-assisted-dev tooling + internal working docs — kept local, excluded from the public repo
CLAUDE.md
.github/workflows/claude-review.yml
.github/workflows/security-review.yml
docs/agent-guidelines/
docs/upstream-monitoring.md
